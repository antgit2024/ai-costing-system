# Costing Task Log

| 日期 | 模块 | 角色 | 任务/问题 | 结论 | 待办 |
| --- | --- | --- | --- | --- | --- |
| 2025-12-22 | ERP口径补强（Guardrails） | Planner-Optimization | 将讨论确认的“6类ERP护栏/口径”固化为单页增补，避免后续开发跑偏或补救成本变高。 | ✅ 新增 `DOC/costing/reviews/erp_guardrails_addendum_20251222.md`（版本为最小核算单元、重跑语义、编码定位、解析版本化、异常工作台、成本口径），并回填 `state/workset/commands` 可检索引用。 | 后续评审/派单必须引用该增补页；若新增成本策略或重跑接口，先更新此页再改代码。 |
| 2025-12-22 | 路线决策（主链优先） | Planner-Optimization | 用户确认优先级：先把“发货单→SKU绑定→spec解析→BOM快照/异常队列→可重试/可重跑”主链跑通，再用“模型套模型+自动编码”扩展解决 30% 复杂产品。 | ✅ 已在评审稿补充“路线决策（先主链后30%）”章节，并同步到 `DOC/agents/state.md`。 | 下一轮建议最小闭环：批量绑定（sku_code→published version）+ 批次重试异常（不回写历史，生成新快照）。 |
| 2025-12-22 | 发货时再解析（评审/优化） | Planner-Optimization | 基于仓库文档评审：回答 `SKU_NOT_BOUND` 产生原因与治理、`spec_text` 不稳定的可追溯策略（spec_hash/解析版本/修订链路）、幂等导入后的重试/重跑语义、UI按 Excel 列展示与系统字段抽屉化。 | ✅ 新增评审稿 `DOC/costing/reviews/shipment_time_parse_review_phase0_phase1_20251222.md`，并回填恢复包 `state/workset/commands/known_issues` 可检索引用。 | Phase0 建议：批量绑定 + 重试异常入口 + 文案与抽屉信息架构；Phase1 建议：解析版本化/修订链路 + 指标化 + 可选埋码治理。 |
| 2025-12-21 | 行级变体部署（47.99.89.206） | Backend Ops | 按 `ops_deploy_line_variants_to_4799` 任务单将行级变体 MVP（后端 + 前端）上线到 8800。 | ✅ `git pull --ff-only` → `alembic upgrade heads`（`0016_line_variants_mvp` 与 `0754ad7d6c3f` 双 head 已用 `heads` 选项同步）→ `systemctl --user restart planner-costing.service`，`curl http://127.0.0.1:8800/api/planner/health` 返回 ok；前端 `npm -C frontend ci && npm -C frontend run build` 并以 `PLANNER_STATIC_DIR=/var/www/html/ai-costing/dist ./scripts/deploy_static.sh` 发布；验收命令 `curl ...openapi.json | python -c ...` 输出 `True`，确认 `/spec/parse`、`/bom/generate`、`/line-variants` 已暴露。 | 持续监控 8800 服务/静态目录；如需彻底消除多 head，请后续合入一个正式 merge revision。 |
| 2025-12-21 | 标准模型（行级变体收口） | Frontend Agent | 将行级变体收口为“ERP 最稳第一步”，避免 items 维护爆炸与对账风险：只允许 replace_self + 同单位 1→1，并把“启用前必须预演成功”做成硬门槛。 | ✅ 仅改 `LineVariantDrawer.tsx`：action UI/保存/新建全部锁死为 `replace_self`；items 编辑限制为 **仅 1 行目标物料**（禁止新增第 2 行）；启用前必须 `spec/parse + bom/generate` 预演成功且不“过期”；若单位不一致/单位缺失（需先保存清单回填/补齐主数据）则禁止启用并提示；UI 展示最近预演时间与结果摘要。 | 无（若要放开复杂动作/1→N/bundle 语义，需另开一轮闭环并补全校验与审计口径）。 |
| 2025-12-21 | 标准模型（变体条件增强） | Frontend Agent | 用户要求“周长/面积/宽/高 + token”为必需判断条件；在不改后端前提下，把后端已支持的 between 条件接入到 Overlay 面板。 | ✅ `LineVariantDrawer.tsx` 增加条件 UI：`width_between/height_between/area_between/perimeter_between`（可选）并写回 `conditions`；纳入“预演过期/启用门槛”——若配置了某个 between 条件，启用前要求预演样例能解析出对应 metric。并在 UI 明确提示“数量/个数条件”当前后端缺字段，需下一轮补后端或用 token 离散化。 | 若业务坚持“数量/个数”必须结构化条件，请开下一轮：后端新增 `quantity_between` 并在 spec parser 或 bom 入口提供数量口径。 |
| 2025-12-21 | 标准模型（行级变体 / 规则录入体验） | Frontend Agent | 用户要求把 Overlay 编辑改成 ERP 风格“多条规则表格”，减少堆叠：触发类型统一选择，下面多行规则（启动/条件表达式/替换物料/β/α/覆盖率/损耗%）可新增删除。 | ✅ `LineVariantDrawer.tsx`：编辑弹窗切换为“同一触发类型下多条规则表格”，支持新增/删除；每行启用前强制要求已预演且不过期（前端直接拦截并提示）；保存时逐行写入 line-variants + 1→1 items 并由后端回填编码/名称/单位。 | 若未来要允许“每行不同触发类型”混排，需要改为按行存 trigger_type 并按行做预演覆盖/单位回填/启用门槛。 |
| 2025-12-21 | 物料管理（缩略图恢复） | Backend Agent | `/costing/materials` 缩略图全部不显示：图片代理接口缺失/回退，且本地落盘文件丢失。要求恢复“钉钉同步到本地保存”的策略。 | ✅ 恢复 `GET /api/planner/base-config/materials/{id}/images/{idx}`：优先读 `metadata_json.local_images` 的本地文件；若缺失则走钉钉 YiDa 附件临时地址链路拉取并落盘到 `PLANNER_MEDIA_DIR`，同时回写 `metadata_json.local_images`。纠偏：`temporaryUrls` 在当前环境为 GET 且 `appType` 在路径中。 | 后续建议：增加定时巡检/补偿任务（扫描 local_images 丢失文件并自动回补），避免迁移/清理磁盘后再次全量丢图。 |
| 2025-12-22 | 行级变体（替换物料选择器 MVP） | Frontend Agent | 替换物料目前要手输 `material_ref_id`，易错且不可读；按 briefing 要求收口为“可搜索选择 + 自动回填 + 展示单位”。 | ✅ `LineVariantDrawer.tsx`：编辑弹窗“替换物料”改为选择器按钮（打开 → 搜索 → 选择），选择后回填 `material_ref_id + code/name/unit`；新增 `MaterialSelectModal.tsx` 复用 `GET /api/planner/base-config/materials`。 | 如要支持“更多筛选/分页/虚拟物料”等扩展，建议后续单独迭代，不混入本轮闭环。 |
| 2025-12-22 | 行级变体（预演数量为 0 兜底修复） | Backend Agent | 用户预演命中 replace_self 规则但 `variant_item computed_quantity=0`（历史规则中替换行 `calculation_method/base_quantity` 常为空或为 0）。 | ✅ `bom/generate`：在 `replace_self` 下，替换行若 `count/β=0/单位缺失` 则继承基准行的计量方式/β/单位，避免用户必须逐条重录旧规则。 | 后续：可在保存 items 时也做同样兜底（落库即正确），并在 UI 提示“已自动继承基准行计量方式”。 |
| 2025-12-22 | SKU→库存扣料清单（派单） | Planner（Hub Agent） | 行级变体与预演链路已基本可用，下一步转向主链路：SKU 绑定到已发布标准版本，解析规格得到尺寸/tokens，并生成最终 BOM 快照用于扣库/出库/核算。 | ✅ 新增 `DOC/agents/briefings/backend_sku_binding_inventory_mvp.md`，并同步到 `workset/state`。 | @Backend Agent 按任务单实现 `POST /sku-bom/generate` 并补最小 pytest 验收。 |
| 2025-12-22 | SKU→BOM→发货/扣库/核算（方案） | Planner（Hub Agent） | 用户提供“SKU绑定与BOM生成说明”与发货单样例，要求以 ERP 视角优化口径与落地计划（先方案后执行）。 | ✅ 产出 `DOC/costing/blueprints/sku_binding_bom_shipment_plan.md`：补齐导入标准化、绑定粒度（SKU→published版本）、解析快照、BOM快照、异常队列、分阶段闭环。并将 xlsx 表头以无依赖方式提炼到 `DOC/index/extracted/shipment_xlsx_extracted_20251222T000000+0800.md`。 | 下一步按阶段 A/B/C 分别闭环；优先做后端 `sku-bom/generate` + pytest。 |
| 2025-12-22 | 发货单导入幂等/去重（方案补充） | Planner（Hub Agent） | 用户确认前期以“每日导出 Excel 导入”为主，需解决：哪些字段参与计算、如何判断文件已导入、如何避免跨天重复统计。 | ✅ 在 `sku_binding_bom_shipment_plan.md` 补充字段过滤策略、文件级幂等（file_hash+import_batch）、行级幂等（external_line_key_hash），以及修订行处理建议。 | 后端落地时：shipment import 作业必须先实现幂等与修订轨迹，否则对账会漂。 |
| 2025-12-22 | 交易规格变更/历史迁移（方案补充） | Planner（Hub Agent） | 用户确认 spec_text 会随店铺调整而变化，且有几十万历史规格需临时规则映射；要求给出可运营的处理方式。 | ✅ 在 `sku_binding_bom_shipment_plan.md` 补充：spec_text 变更检测（spec_hash/修订/解析版本化/快照不回写）、历史迁移两线并行（SKU绑定优先+临时解析规则库）、新订单运营规则（软约束→硬约束）。 | 下一步：后端落地 shipment import 时同步落 spec_hash/parse_snapshot，并建立异常队列与修订链路。 |
| 2025-12-22 | 发货导入时解析（派单） | Planner（Hub Agent） | 用户确认“发货时再解析”作为初始目的：日活约 1000 行且重复多，适合 spec_hash 缓存；需要异常队列兜底未绑定/解析失败。 | ✅ 新增后端闭环任务单 `DOC/agents/briefings/backend_shipment_import_bom_snapshots_mvp.md`（导入 xlsx→标准化/幂等→spec_hash缓存→BOM快照→异常队列）。 | @Backend Agent 按任务单闭环实现并跑 pytest 验收。 |
| 2025-12-22 | 发货单导入→BOM快照（MVP） | Backend Agent | 按 `backend_shipment_import_bom_snapshots_mvp` 实现：导入 xlsx → 标准化行落库 → 文件级幂等 + 行级幂等 → spec_hash 缓存解析 → 生成 BOM 快照（含 trace）或入异常队列。 | ✅ 新增表 `shipment_import_batches/shipment_lines/spec_parse_snapshots/bom_snapshots/shipment_exception_queue` 与 API：`POST /api/planner/shipments/import`、`GET /shipments/import-batches/{id}`、`GET /shipments/exceptions?batch_id=...`、`GET /shipments/bom-snapshots?batch_id=...`；spec cache 以 `sha1(spec_text)` 去重并带 `parser_version=v1`；BOM 快照 trace 回填 `bound_version_id/spec_hash`；新增单测 `pytest backend/tests/planner/test_shipment_import_bom_snapshots_mvp.py -q` 通过。 | 如需更强容错（列位移/多 sheet/表头变体）与“修订链路”展示，后续单开迭代；线上数据库如存在历史 revision（例如 `0754ad7d6c3f`）需补齐 merge revision 或用 `alembic upgrade heads` 策略治理。 |
| 2025-12-22 | Alembic 缺失 revision 兜底（部署解阻） | Backend Agent | 目标机 DB 的 `alembic_version` 可能已存在 `0754ad7d6c3f`，但仓库缺该 revision 文件，导致 `alembic upgrade` 报错：`Can't locate revision identified by '0754ad7d6c3f'`。 | ✅ 新增 no-op stub `backend/migrations/versions/0754ad7d6c3f_stub.py`（提交 `247c054`）以补齐 revision 图谱，允许按既定口径继续执行 `alembic upgrade heads`。 | 后续建议：在合适窗口补一个正式 merge revision（线性化迁移），避免长期依赖 stub。 |
| 2025-12-22 | 发货导入→BOM快照（上线派单） | Planner（Hub Agent） | 后端 MVP 已落地并本地 pytest 通过，需要上线到 47.99.89.206（包含迁移与路由暴露校验）。 | ✅ 新增运维闭环任务单 `DOC/agents/briefings/ops_deploy_shipment_import_bom_snapshots_to_4799.md`（git pull→alembic upgrade heads→重启→curl openapi→pytest 复跑）。 | @Ops Agent / @Backend Ops 按任务单闭环上线并回填 state/task_log。 |
| 2025-12-22 | 发货导入→BOM快照（部署到 47.99.89.206） | Backend Ops | 按 `ops_deploy_shipment_import_bom_snapshots_to_4799` 上线：拉取含 `247c054` 的代码，处理多 head，用 `alembic upgrade heads` 升级并重启 user-scope 服务，验证 OpenAPI 路由与 pytest。 | ✅ `git pull --ff-only` 后 `alembic heads` 显示 `0017_shipment_import_bom_snapshots_mvp` 与 `0754ad7d6c3f` 双 head；`alembic upgrade heads` 成功运行 `0016 -> 0017`；重启服务需设置 `XDG_RUNTIME_DIR=/run/user/$(id -u)` 后 `systemctl --user restart planner-costing.service` 成功；`curl .../openapi.json` 校验 shipments 路由输出 `True`；`pytest backend/tests/planner/test_shipment_import_bom_snapshots_mvp.py -q` 输出 `1 passed`。 | 后续如要彻底治理多 head：补一个正式 merge revision（线性化迁移），避免长期依赖 stub。 |
| 2025-12-23 | SKU 主档导入 + 发货回写（MVP） | Backend Agent | 按 `backend_sku_master_import_and_autobind_mvp`：新增 `sku_master` 主档表，支持从 `ERP 理 平台商品列表.xlsx` 导入（按货品条码 upsert），并在发货导入时若未命中主档则用发货行回写最小主档供下次命中。 | ✅ 新增 `sku_master` 表（`erp_sku_barcode` unique）；新增 API `POST /api/planner/sku-master/import`、`GET /api/planner/sku-master`、`GET /api/planner/sku-master/{id}`；增强 `POST /api/planner/shipments/import`：barcode 不存在则创建 `metadata.source=shipment_autobackfill` 的最小主档，不覆盖已存在主档；单测 `pytest backend/tests/planner/test_sku_master_import_mvp.py -q` 通过。 | 下一步若要“自动绑定标准版本/模型提示（model_hint）”再单开迭代；本轮不扩范围。 |
| 2025-12-23 | SKU 主档绑定工作台（模型→唯一发布标准版本） | Planner+Backend+Frontend | 用户口径确认：一个标准模型只能有一个在线发布版本；因此手工绑定应“只选模型，不选版本”，系统自动落到该模型唯一 `published standard` 版本；同时提供自动绑定预览/执行（仅 model_code_hint 唯一命中）。 | ✅ 后端新增：`GET /api/planner/sku-master/published-standard-models`（候选下拉）、`POST /api/planner/sku-master/bind-by-model`（批量绑定，不覆盖已有绑定）、`POST /api/planner/sku-master/auto-bind/preview|execute`；并修复 SQLite 不支持 `NULLS LAST` 的排序兼容；新增单测 `pytest backend/tests/planner/test_sku_master_binding_workbench_mvp.py -q` 通过。✅ 前端：`/costing/sku-master` 左侧工作台接入“模型下拉 + 勾选批量绑定 + 自动绑定预览/执行”，右侧支持(全部/未绑定/规格差异)筛选与勾选；`npm -C frontend run build` 通过。 | 后续：自动绑定可补“预览列表明细/可回滚标记”；如要强制模型编码前缀校验，可在绑定入口保留开关（默认对条码跳过）。 |
| 2025-12-23 | 标准模型（型号识别规则 / 关键词映射） | Planner+Backend+Frontend | 用户提出：自动绑定不应只靠“猜模型码”，应在标准模型管理中维护“型号识别/关键词映射”（如 OZU=丝圈地垫/丝圈），并要求关键词唯一，避免冲突；自动链路先识别模型再进入行级变体。 | ✅ 后端：`product_model_service.update_model` 增加 `metadata_json.recognition_keywords` 的全局唯一校验（仅对存在 `published standard` 的模型集合参与）；`sku-master auto-bind preview` 增加按关键词匹配交易规格（匹配方法 `model_keyword`，返回 `match_method/matched_keyword`），并保留 `model_code_hint` 兜底。✅ 前端：标准模型编辑抽屉新增 Tab **“型号识别规则”**，用 tags 方式维护关键词并调用 `PATCH /api/planner/product-models/{id}` 保存。`npm -C frontend run build` 通过。 | 后续：补“预演输入框 + 歧义提示列表”；把 preview/execute 的返回在前端展示明细（命中关键词/方法/将绑定到哪个版本）。 |
| 2025-12-23 | 型号识别规则（列表式编辑 + 校验按钮） | Planner+Backend+Frontend | 用户反馈 tags 直接保存不利于运营：需要列表式维护、可删除，并支持“校验/保存”分离。 | ✅ 前端：`型号识别规则` Tab 改为列表式（新增/删除/校验/保存），校验结果用 Alert 展示冲突明细。✅ 后端：新增 `POST /api/planner/product-models/{id}/recognition/validate`（不落库，返回 normalized_keywords + conflicts）。新增单测 `pytest backend/tests/planner/test_product_model_recognition_validate.py -q` 通过；前端 `npm -C frontend run build` 通过。 | 后续：可把“预演 spec_text 命中哪个模型/命中词”也放到该 Tab，形成闭环运营面板。 |
| 2025-12-23 | SKU 主档（自动绑定：预览候选列表 + 只绑定选中） | Planner+Backend+Frontend | 用户反馈：预览显示“命中=200”但右侧列表看不到/无法控制；期望预览后自动过滤命中候选并默认选中，执行时只绑定被勾选的记录，并预览显示匹配模型信息。 | ✅ 前端：自动预览后右侧进入“命中候选视图”（仅显示候选并默认全选），新增列展示匹配模型/命中词/方式；执行时若未勾选或勾选不在候选集合则直接拦截。✅ 后端：`POST /api/planner/sku-master/auto-bind/execute` 支持 `sku_master_ids` 入参，仅绑定指定候选；并在 mapping.metadata 记录 `binding_method/matched_keyword`。单测 `pytest backend/tests/planner/test_sku_master_binding_workbench_mvp.py -q`、前端 `npm -C frontend run build` 均通过。 | 后续：可扩展预览页分页/导出，或在右侧提供“只看候选/恢复全部”更显眼的开关。 |
| 2025-12-21 | 行级变体（条件落库修复） | Backend Agent | between 条件（perimeter/area/width/height）在创建/更新规则时返回 500：`conditions_json` 写入 JSON 列包含 `Decimal/tuple` 无法序列化。 | ✅ `line_variant_service` 增加 `_json_safe`（Decimal→字符串、tuple→list）并在 create/update 时统一处理 `conditions/metadata`，使 between 条件可正常落库。 | 若后续还出现 JSON 序列化问题（例如 metadata 内嵌 Decimal），复用 `_json_safe` 统一治理。 |
| 2025-12-22 | 行级变体（前端UX派单） | Planner（Hub Agent） | 变体已可用但“替换物料需手输ID”风险高、难用：派发前端闭环把替换物料改为选择器（搜索/选择/自动回填编码名称单位），并保持同单位 1→1 与预演门槛。 | ✅ 新增 `DOC/agents/briefings/frontend_line_variants_material_picker_mvp.md`，并同步到 `workset/state`。 | @Frontend Agent 按任务单闭环实现并跑 `npm -C frontend run build`。 |
| 2025-12-21 | 后端服务恢复（ImportError） | Backend Ops/Agent | `planner-costing.service` 重启后 ImportError（缺失 `variant_rule_service` / `codes` / `process_module_service` 等模块），导致 8800 端口不监听。 | ✅ 从历史暂存恢复并纳入版本控制：补齐 `variant_rule_service`、关键 routers（codes/processes/process_modules/product_models/product_model_versions）及依赖 services/utils（code_generator_service/process_module_service/process_service/unit_normalizer）；重启后 health 恢复为 ok。 | 建议后续清理“未追踪文件依赖”的风险：所有运行必需模块必须进 Git，避免重启即崩。 |
| 2025-12-21 | 协作方法论（可复用） | Planner（Hub Agent） | 将本仓“Hub Agent 派单 + 执行 Agent 闭环 + 恢复包/工作集/提炼件”机制整理成跨项目可复用的标准说明，便于新项目快速落地规则与目录结构。 | ✅ 新增 `DOC/agents/task_distribution_standard.md`：包含角色边界、必备文件/目录、强制规则、派单模板、验收/提交/恢复流程、新项目落地步骤。并已加入 `DOC/agents/workset.md`。 | 建议新项目初始化时先复制 `DOC/agents/` 目录骨架，再按项目特点填充 workset/commands。 |
| 2025-12-21 | Agent 体系/接力 | Planner（Hub Agent） | 固化“Hub Agent + 执行 Agent”最稳最低成本协作架构，并将闭环任务单模板写入接力包；修正本轮验收命令为系统默认可用的 `grep -n`。 | ✅ `DOC/agents/handoff_planner.md` 已补齐推荐架构与任务单模板；`DOC/agents/state.md` 已回填本轮产物与可执行验收命令；`grep` 验证通过。 | 后续每轮 Hub Agent 产出“下一步闭环任务单 + 验收命令 + 依赖契约”，执行 Agent 回填 state/task_log。 |
| 2025-12-21 | Agent 派单规范 | Planner（Hub Agent） | 为避免执行 Agent 不清楚自身身份/交付门槛，将“每轮必须自维护+硬验收+Git落地”三件事固化为派单统一尾注，并要求派单首行明确 `@Frontend/@Backend/@Docs Agent` 身份。 | ✅ `DOC/agents/handoff_planner.md` 新增“派单统一尾注（每次派活必贴）”；`DOC/agents/state.md` 更新本轮验收命令指向该标题。 | 建议后续把此尾注作为所有派单消息的默认结尾，执行 Agent 完成/暂停前照单执行并回填恢复包。 |
| 2025-12-21 | Hub Agent 自检 | Planner（Hub Agent） | 用户担心 Hub Agent 也会忘规则：将“Hub Agent 派单 SOP/自检清单”固化到接力包，便于每次派活时由用户回贴提醒。 | ✅ `DOC/agents/handoff_planner.md` 新增“Hub Agent 派单 SOP / 自检清单”；`DOC/agents/state.md` 回填本轮产物与验收命令（grep）。 | 后续派单时先贴自检清单，再给执行 Agent 派单任务单，严格一轮一个闭环。 |
| 2025-12-21 | 产品模型（推导标准） | Frontend Agent | 按需求在打样管理面板增加“推导标准模型”入口，并让推导后可在标准模型列表出现并一键进入编辑；标准入口“标准版本”TAB 改为表格展示关键指标列。 | ✅ `ProductModelEditorDrawer` 在“同步物料价格”前新增“推导标准模型”；推导成功可跳转 `/costing/standard-models` 自动打开对应版本；标准版本 TAB 列包含：模型版本号/物料数/工序数/物料价/工序价/制造费/合计价/创建时间/操作。前端 `npm -C frontend run build` 通过。 | 如需将“推导标准模型”也放到打样版本列表行内（已存在“生成”按钮）统一文案，可后续做一次 UI 文案收口。 |
| 2025-12-21 | 标准模型（变体/动态BOM） | Planner（Hub Agent） | 阅读《BOM动态生成引擎业务需求说明》，输出“标准模型变体/动态BOM”可落地方案：SKU绑定→规格解析→**行级变体（每物料行变体图标 + 变体物料清单表）**→最终BOM输出；预推画框替换/推拉新增/无框删除等场景，给出冲突与执行顺序。 | ✅ 更新方案文档 `DOC/costing/blueprints/standard_model_variants_plan.md`：不再强依赖通用 `virtual_kind=kit` 承载物料组，改为 version-scoped 行级变体（replace_bundle/remove_self/add_siblings），并给出数据模型/API/UI/验收用例/分期。 | 下一步按方案拆分 Backend/Frontend/Docs 单轮闭环任务单（spec/parse、line_variants CRUD、bom/generate、SKU待绑定工作台）。 |
| 2025-12-21 | 标准模型（变体UI确认） | Planner（Hub Agent） | 用户确认交互：物料行“变体”按钮打开独立面板，面板内管理变体清单；基准标准清单保持不变（overlay），用于动态生成/预览/订单出料。 | ✅ 方案文档补充“独立面板 + overlay 不改基准清单 + 插入位置(锚点行)”规则。 | 下一步前端可按此交互实现 Drawer/Modal；后端按 version-scoped line_variants 存储。 |
| 2025-12-21 | 行级变体（派单） | Planner（Hub Agent） | 用户确认“锚点行”方案：ADD 类变体统一挂锚点行，插入位置固定为“锚点行之后”。据此产出后端最小闭环任务单（spec/parse + line_variants CRUD + bom/generate MVP）。 | ✅ 新增 `DOC/agents/briefings/backend_line_variants_mvp.md`；`workset/state/task_log` 同步更新。 | @Backend Agent 按任务单闭环实现，并用 pytest 单测作为唯一验收命令。 |
| 2025-12-21 | 行级变体（前端派单） | Planner（Hub Agent） | 后端 line-variants/spec/parse/bom MVP 已完成，开始派发前端闭环：标准模型清单每行“变体”按钮→独立 Drawer 管理变体清单（overlay，不改基准清单）→spec_text 预演展示 tokens 与最终 BOM。 | ✅ 新增 `DOC/agents/briefings/frontend_line_variants_panel_mvp.md`，并同步到 `workset/state/task_log`。 | @Frontend Agent 按任务单实现 UI 与 API 对接，验收命令：`npm -C frontend run build`。 |
| 2025-12-21 | 行级变体（前端闭环） | Frontend Agent | 标准入口“清单编辑”Tab 为每条物料行新增“变体（Overlay）”按钮，点击打开独立 Drawer 管理 version-scoped line-variants（不改基准清单）；Drawer 内支持 `spec_text` 预演：展示 `/spec/parse` tokens 与 `/bom/generate` 最终 BOM + trace。 | ✅ 新增 `frontend/src/components/costing/LineVariantDrawer.tsx`；`ProductModelEditorDrawer.tsx` 接入入口；`frontend/src/services/planner.ts` 补齐 spec/bom/line-variants API；`frontend/src/types/planner.ts` 补齐类型；并按规则更新 `DOC/agents/state.md/workset.md`。 | `npm -C frontend run build` |
| 2025-12-21 | 行级变体 / Spec Parser / BOM | Backend Agent | 落库 `product_model_line_variants` + `variant_items` 结构，实现 version-scoped 行级变体 CRUD、`POST /api/planner/spec/parse`（tokens + 尺寸 + explanations）、`POST /bom/generate`（锚点行 overlay + trace 输出），并补充服务/路由/README/单测。 | ✅ Alembic `0016_line_variants_mvp`、`spec_parser_service`、`line_variant_service`、`bom_generation_service`、`routers/specs|line_variants|bom`、`schemas` 全面上线，README/DOC/State/Task Log 同步说明；新增 `pytest backend/tests/planner/test_line_variants_mvp.py -q` 覆盖 CRUD+生成链路，`npm -C frontend run build` 验收通过。 | 待办：前端实现行级变体面板与 BOM 预览；将新代码部署至 47.99.89.206（git pull → alembic upgrade head → systemctl --user restart planner-costing.service → `curl http://47.99.89.206:8800/openapi.json` 校验 /spec|/line-variants|/bom）。 |
| 2025-12-21 | 行级变体 / 审计日志 | Backend Agent | 行级变体单测命中 `Decimal` 无法写入审计日志、替换后的变体行丢失 `material_code`，导致动态 BOM 输出字段为空。 | ✅ `audit_service` 新增 `_json_safe` 递归处理（Decimal/日期/UUID → 字符串），审计 payload 可直接落 JSON；`line_variant_service` 在保存 items 时自动回填引用物料的编码/名称/单位/计量方式并校验真实/虚拟物料存在性。`pytest backend/tests/planner/test_line_variants_mvp.py -q`、`npm -C frontend run build` 均通过。 | 待办：部署前重跑 `alembic upgrade head` + `systemctl --user restart planner-costing.service`，并在 47.99.89.206 复现 `pytest backend/tests/planner/test_line_variants_mvp.py -q`；若后续支持 BOM 类型引用需扩展 `_normalize_item_payload`。 |
| 2025-12-21 | 行级变体（上线派单） | Planner（Hub Agent） | 前后端行级变体 MVP 已完成（后端 a7ee7ac/f58162a，前端 73f4996），派发运维闭环：部署到 47.99.89.206 并用 curl 校验 OpenAPI/关键路由。 | ✅ 新增 `DOC/agents/briefings/ops_deploy_line_variants_to_4799.md`，并加入 `workset/state/task_log`。 | @Ops Agent / @Backend Agent 按任务单完成上线与回填（state/task_log）并小步提交。 |
| 2025-12-21 | 行级变体（运营规范） | Docs/Planner | 将“变体规则的 ERP 化管控”沉淀为运营/实施规范：token 字典化、尺寸边界离散化、上线前预演留痕、变更控制流程、扣库存以最终 BOM 快照为准。 | ✅ 新增 `DOC/costing/manuals/standard_model_variants_ops_rules.md`，并更新 `workset/state/commands` 便于新同事快速查阅。 | 运营按规范维护 token 词典与边界策略；后续若上线“组级替换（G001→G002）”再单开迭代。 |
| 2025-12-21 | 变体讨论提炼与收口 | Planner/Docs | 导出日志中关于“变体收口（同计量单位 1→1 平替、默认只 replace_self、规则上限）”的后续讨论未进入规范，现按规则提炼到 `DOC/index/extracted/` 并回写运营规范，避免以后再读导出全文。 | ✅ 新增提炼件 `DOC/index/extracted/variants_discussion_extracted_20251221T200250+0800.md`；`standard_model_variants_ops_rules.md` 补充“最稳第一步”与“每物料规则/条件上限”章节；`workset/state` 同步更新。 | 下一步如要把“只允许 replace_self + 同单位校验”落实到 UI/后端强校验，需单开一轮闭环。 |
| 2025-12-21 | 前端接力包刷新 | Planner（Hub Agent） | 前端再次崩溃后，整理“行级变体（Overlay）+ 运营规范口径 + 常见坑(base_line_id)”为新 Frontend Agent 一页接力包，确保不依赖聊天即可接手。 | ✅ 更新 `DOC/agents/handoff_frontend.md`：增加行级变体关键文件/接口/坑位与运营规范引用，删除重复的“替代已崩前端”冗余段落。 | 新 Frontend Agent 接手时按 `handoff_frontend.md → state.md → commands.md` 顺序阅读并开工。 |
| 2025-12-19 | 成本核算-产品模型 | Frontend Agent / Backend Agent | 根据反馈调整 `/costing/models`：将“标准模型”Tab更名为“打样模型”；新增“标准模型”Tab入口；将“工艺路线/占位符映射”合并到打样模型左侧；版本管理列表补充 1×1㎡ 预览汇总列；后端 draft version 自动生成可读 `version_label`。 | ✅ 前端构建通过；后端单测通过；文档 `ui_specs/product_model.md` 同步更新。 | 若目标环境仍出现 `/product-models/{id}/sync-from-modules` 500，请部署新版后端（改为 version endpoint）并回填错误 detail。 |
| 2025-12-11 | 项目初始化 | Planner | 在 `/home/admin/ai-costing-system/` 创建独立项目骨架（backend/frontend/DOC/agents）。 | 完成目录创建；等待下一步需求整理 | 建 `requirements.md`、制定迭代计划 |
| 2025-12-12 | 部署准备 | Backend Ops | 创建/激活 venv，安装依赖，复制 `.env` 并填写数据库/关键变量；运行 `validate_env.py` | ✅ venv 建立；`pip install -r requirements.txt` 完成；`.env` 指向 MySQL rm-bp1y780gkt8s37kvk；`validate_env.py` 通过 | 需要有 sudo 权限才能 `systemctl restart planner-api.service`（当前账户无权限），待提供或改用 user-level service |
| 2025-12-12 | 服务运行 | Backend Ops | 因无 sudo 权限，改为创建 user-level systemd：`~/.config/systemd/user/planner-costing.service` (uvicorn src.main:app --port 8800) | ✅ `systemctl --user enable --now planner-costing.service`，进程 2716639 运行，健康检查 `GET /api/planner/health` 返回 ok；额外安装 `pymysql` 以支持 MySQL | Smoke/API 场景尚未执行；需确认 8800 端口是否可对外暴露或加反向代理；如仍需系统级 service，请提供 sudo 权限 |
| 2025-12-13 | 数据库切换 | Backend Ops | 按需改为使用 PostgreSQL RDS (`pgm-bp1w3y58xiiq163w.pg.rds.aliyuncs.com:5432/ai-costing-system`)，安装 `psycopg2-binary`、更新 `.env`、重跑 Alembic（修复 boolean 默认值） | ✅ 重新创建数据库、运行 `alembic upgrade head` (Postgres)、重启 user-level service；`GET /api/planner/initiatives` 返回空列表 (200) 表示链路打通 | 下一步执行 Smoke：CSV 导入 / 场景克隆 / 审批 / 导出 / AI 收藏；监控 `/metrics`、Kafka lag、benchmark fallback；需准备示例数据/调用脚本 |
| 2025-12-13 | Smoke 验证 | Backend Ops | 执行部署指引：CSV 导入 → 场景克隆 → 审批提交/通过 → 导出 → Benchmark 建议/收藏；采集 `/metrics` | ✅ 新建 `INIT-SMOKE` → 导入 2 条 line item（job `1b3e7498...` completed）；克隆 baseline 生成 `SCN-SMOKE`；审批流程从 `planner_user`→`approver_user` 通畅；导出返回 `MOCK-...` reference；`/benchmarks/suggest` 返回 mock；`/metrics` 看到 `planner_executor_requests_total{status="success"}=1`，Kafka 未真实连接（mock mode）；Benchmark source=mock | 下一步：准备真实 executor/benchmark/Kafka/Redis 配置并在非 mock 模式下复测；补充 CSV 示例到 DOC；视需要加自动化脚本 |
| 2025-12-13 | feature/costing-planner | Planner | 梳理planner需求、数据模型和交付里程碑。 | 产出 `DOC/costing/requirements.md` v0.1，涵盖业务目标、数据表和阶段计划 | 待确认业务假设、补充ERD与API细节 |
| 2025-12-13 | feature/costing-planner | Planner | 将 Planner 需求文档补充中文详版，便于团队评审。 | `DOC/costing/requirements.md` 新增“附录：中文说明（详版）”覆盖全部章节 | 若需逐条翻译或修订章节，等待业务反馈 |
| 2025-12-13 | feature/costing-planner | Planner | 记录业务确认结果（货币、权限、附件、AI 数据、性能）并制定分支策略。 | `requirements.md` 更新第 7 节决策与第 8 节分工分支说明 | 后续分支下可按子任务继续拆解 |
| 2025-12-13 | feature/costing-backend | Backend Agent | Phase 0/1 planner 后端：FastAPI 模块、迁移、CRUD、CSV 导入、供应商校验、假设服务。 | 新建 `backend/src` 模块、`0001_init` 迁移、initiatives/packages/line-items/assumptions API、异步导入任务（含校验）、单测 `pytest tests/planner -q` 全绿。 | 后续 Phase 2 需实现场景快照/审批流；完善异步任务队列与实际消息代理。 |
| 2025-12-13 | feature/costing-backend | Backend Agent | Phase 2：场景快照、差异分析、审批流、通知钩子。 | 新增 snapshot/diff/approval/job Router、`snapshot_service`、`notification_service`，引入 `0002_phase2_jobs` 迁移、`/scenarios/*` & `/approvals/*` & `/jobs/{id}` API，扩展作业表支持 `scenario_snapshot`/`scenario_diff`，完成单测覆盖（含 clone/diff/approval）。 | 后续需接入真实消息总线/权限系统、完善多场景对比 UI、补充更多审批角色规则。 |
| 2025-12-13 | feature/costing-backend | Backend Agent | Phase 3：场景导出、AI Benchmark Hook、审计日志。 | 新增 `audit_logs` 表与 `audit_service`、`executor_client` mock，提供 `/scenarios/{id}/export`、`/benchmarks/suggest`、审计打点（场景/审批/导出），PlannerJob 支持 `scenario_export`，对应测试 `test_phase3.py` + README 更新。 | 后续接入真实执行系统与基准服务；审计字段可扩展 trace_id/环境信息。 |
| 2025-12-13 | feature/costing-backend-phase3-5 | Backend Agent | Phase 3.5 生产集成：执行器、场景列表/收藏、AI Benchmark、消息总线、审计/指标。 | 创建子分支 `feature/costing-backend-phase3-5`，落地真实 executor HTTP 客户端（重试/断路器/回调签名）+ `/executor/callback`、`scenario_favorites` & `benchmark_favorites` 表、`/scenarios` 列表/收藏、AI Benchmark 代理+缓存+收藏、Kafka/RabbitMQ 事件钩子、`planner_executor_callbacks` 追踪表、Prometheus `/metrics`、迁移 `0004_phase35_integrations.py`、集成测试 `pytest tests/planner -q`（11 passed）。 | 风险：消息总线默认降级到内存，生产需配置 broker；Benchmark/Executor 连接依赖外部可用性；Redis 缓存可后续替换 TTLCache；需在 Docs/QA 同步回调密钥、env 样例与回滚策略。 |
| 2025-12-13 | feature/costing-frontend | Frontend Agent | Phase 0/1 Planner UI：/planner 路由、initiative/包树/行表、CSV 导入、Scenario Builder 占位。 | 新建 React+Antd 前端，集成 React Query+Zustand；完成 Planner 工作区骨架与 inline edit、导入任务抽屉；补充 Scenario Builder 占位和 vitest+store 测试。 | 风险：后续需补充真实场景数据与权限校验；CSV 大文件导入需观察后台性能，必要时增加任务列表页。 |
| 2025-12-13 | feature/costing-frontend | Frontend Agent | Phase 2 场景克隆/差异分析/审批 UI + 统一作业抽屉。 | `/planner/scenario-builder` 接入 clone/diff/approval/job API；实现场景列表、差异表格、审批表单与 job drawer；CSV 导入复用统一作业抽屉，补充文档与测试。 | 遗留：暂无后端场景列表 API，当前需手动录入 ID；差异/克隆作业大批量时建议后续引入全局 job 列表或通知机制。 |
| 2025-12-13 | feature_costing_frontend_phase3-5 | Frontend Agent | Phase 3.5 生产体验：场景列表、导出历史、AI Benchmark 收藏、懒加载抽屉。 | 新增 `/planner/scenarios` 列表（筛选/收藏/批量操作）、Scenario Builder 接入真实场景与导出历史、AI Benchmark 收藏持久化、Planner/Audit Drawer 懒加载+Trace ID+搜索复制；文档/手册/README 更新并附打包报告。 | 风险：场景列表依赖后端分页性能；AI 建议仍由外部服务提供，如延迟较大需引入缓存提示；`PlannerJobDrawer` 子包仍 ~185 kB，后续可考虑进一步拆分。 |
| 2025-12-13 | feature/costing-frontend | Frontend Agent | Phase 3 场景导出、AI Benchmark、审计日志、bundle 优化。 | Scenario Builder 新增导出任务(`/scenarios/{id}/export`)、AI Benchmark 面板(`/benchmarks/suggest`)、审计日志 Drawer(`/audit`)，并复用 PlannerJobDrawer；React.lazy + `vite-plugin-imp` 降低首包体积；完善服务/测试/文档。 | 遗留：AI 建议当前为占位数据，等待真实基准模型；审计列表依赖后台分页；可增加场景列表 API、收藏持久化。 |
| 2025-12-13 | feature/costing-docs | Docs Agent | Phase 4 文档：架构/手册/交接/QA 回归脚本 | 更新 `DOC/costing/architecture.md`（新增 Phase3.5 模块、流程图、生产部署 checklist）、`DOC/costing/manuals/planner_user_guide.md`（场景列表/导出历史/Trace/SSE/Known Issues）、`DOC/costing/handovers/rd_handoff_phase3.md`（phase4 运行手册、监控/回滚/known issues）、`DOC/costing/qa/phase4_regression.md`（staging 脚本+缺陷）。 | 下一步：待 AI Provider 修复 500、审批通知延迟补救后，再更新 Known Issues；关注消息总线监控配置。 |
| 2025-12-13 | feature/costing-docs | Docs Agent | 补齐架构说明、用户手册、Phase3+ 研发交接资料，并记录操作日志。 | 更新 `DOC/costing/architecture.md`（新增阶段架构、Mermaid 模块/流程图）、撰写 `DOC/costing/manuals/planner_user_guide.md`、`DOC/costing/manuals/export_ai_audit_manual.md`、`DOC/costing/handovers/rd_handoff_phase3.md`，生成所需截图 SVG、QA 样例数据、回归计划；task log 同步。 | Phase 4 需跟进真实执行器/AI 接入与权限/通知方案，届时再扩展交接文档与截图。 |
| 2025-12-13 | feature/costing-planner | Planner | 制定 Phase3.5 生产集成计划并下发 backend/frontend briefing。 | `requirements.md` 新增 Phase3.5 段落，新增 `DOC/agents/briefings/backend_phase35.md` 与 `frontend_phase35.md` | 等待执行结果，后续需同步 Docs/QA |
| 2025-12-13 | feature/costing-planner | Planner | 制定 Phase4 上线冲刺计划并准备 backend/frontend/docs+QA briefing。 | `requirements.md` 新增 Phase4 描述，新增 `backend_phase4.md`、`frontend_phase4.md`、`docs_phase4.md` briefing | 等待各执行角色执行 Phase4 任务 |
| 2025-12-13 | Go-live 准备 | Planner | 汇总上线资料并规划上线窗口、回滚窗口及沟通路径。 | `DOC/costing/go_live_index.md` 更新审批结论；上线窗口：2025-12-14 00:00–02:00 CST；联系人：Zhang Wei/Liu Fang/Chen Qiao；审批包包含架构、手册、runbook、QA、回滚指南。 | 部署后需按 runbook 观测 fallback/通知告警并回填结果 |
| 2025-12-13 | feature/costing-frontend-phase4 | Frontend Agent | Phase 4 UAT（.env.production、性能拆包、真实联调、可观测性、文档截图）。 | 新增 `env.production.example` + README/UAT 指南；按需引入 AntD icons、Rollup `manualChunks`、SSE hook，主入口 chunk 降至 <400 kB；Scenario List/Builder 接入 staging 提示与 offline/timeout Alert、AI 收藏/导出历史实测，通过 Job Drawer/Audit Drawer Trace ID 快速复制；更新手册截图及 task log；`npm run test -- --run`、`npm run build`、`npm run build:report` 均通过。 | 待生产验证：SSE endpoint 需在网关放行；真实 WebSocket 通知仍规划中；如需进一步压缩 bundle，可考虑拆分 AntD 表单组件或启用 React Compiler。 |
| 2025-12-13 | feature/costing-qa | QA Agent | Phase 4 staging 回归（CSV→克隆→差异→审批→导出→AI 收藏→Kafka 事件） | 依据 `DOC/costing/qa/phase4_regression.md` 完成全链路验证；Backend 修复 Q-001（AI Provider fallback）与 Q-002（审批通知延迟）后复测通过：`TRACE-AI-FALLBACK-02` 显示 source=fallback+告警日志，`TRACE-APPROVE-02` 通知耗时 <12s。 | 持续监控 fallback 告警与通知延迟指标；若外部服务再次不稳定，按 runbook 切换 feature flag 或降级。 |
| 2025-12-13 | Go-live Docs | Docs Agent | 汇总上线资料并出具审批索引 | 终版资料包含：`architecture.md`、`planner_user_guide.md`、`manuals/export_ai_audit_manual.md`、`handovers/rd_handoff_phase3.md`（运行/回滚）、`qa/phase4_regression.md`、`go_live_index.md`；`task_log` 已记录 QA 复测及上线准备完成。 | 上线后需持续更新 runbook/回归文档，如监控指标或流程变更请及时补充索引。 |
| 2025-12-14 | Go-live 执行动作 | Ops/Docs 联合 | 上线窗口 00:00–02:00 CST 完成部署 & 验收 | Backend smoke：job `ac7c0c44-1de4-4ea9-8a60-2c4c83d1b8f1`（CSV）、`3d0f0d4a-c1d1-4d58-8bfc-6d5d5f1c1c44`（clone）、`4b22a9c1-6d08-4a6b-938a-8cb8d4ee18e4`（export）；AI 收藏 trace `TRACE-LIVE-AI-01`（favorite `fav-a0f52c5f-2af7-4b21-b1d9-84de9946a2de`）。Prometheus/Kafka/Sentry 均绿色。 | 风险：`/api/planner/audit` 尚未开放→Audit Drawer/导出历史暂不可用；收藏依赖最新前端 build，若使用 preview 需重新部署；若 fallback/通知延迟告警触发，立即通知 Zhang Wei / Liu Fang / Chen Qiao 并按回滚脚本切换 mock。 |
| 2025-12-14 | feature/costing-backend-phase4 | Backend Agent | QA Q-001/Q-002 修复（Benchmark fail-open、通知发布重试）。 | 添加 `BENCHMARK_FAIL_OPEN` fail-open 策略、router fallback，Benchmark 失败自动返回 fallback 且有单测；`notification_service`→`message_bus` 发布支持重试+超时+告警，README/requirements/architecture/runbook/rollback 文档更新，`scripts/validate_env.py` & `run_load_test.py` 输出并附监控指南；`pytest tests/planner -q` 12 通过，QA 可重跑 Q-001/Q-002。 | 待办：QA 重新验证，若外部 Kafka 仍存在 lag 需队列团队扩容或优化消费端。 |
| 2025-12-14 | Go-live 审批 | Planner | 汇总 `go_live_index.md` 资料并生成上线审批包。 | 新增 `DOC/costing/go_live_package.md`，涵盖资料索引、上线窗口、执行 checklist、风险/联系人，便于审批会一键查阅。 | 部署完成后需回填 Smoke Trace 与监控结果，并在审批包中更新。 |
| 2025-12-14 | Smoke – CSV 导入 | Planner | 在 initiative `1a9993c8-12ad-4a7d-96fd-8802f6f6f387` 下创建 `PKG-MAT-001/PKG-LABOR-001/PKG-LOG-001/PKG-SVC-001` 等包，并将 `line_items_smoke.csv` 中 `package_id` 替换为真实 UUID 后重新导入。 | 任务 `9f8b0861-006d-4db1-9510-df858040f088` 状态从 `completed_with_errors` 修复为成功，导入 6 条行项目（铝型材外框/装配工时/海运整柜/质检/PCB/注塑件）；UI 与 API 均可看到数据。 | 下一步：继续执行场景克隆→审批→导出→AI 收藏 Smoke，并记录 Trace/监控。 |
| 2025-12-14 | Smoke – 场景链路 | Planner | 通过 API 跑完克隆→审批→导出→AI 收藏，采集 job/trace。 | 克隆基线 `0e3b8f58…` → job `fb89076d…`（trace `537ff9c1…`）生成 `SCN-SMOKE-CLONE`; 提交 trace `c46d3aaa…`、审批 trace `3fde7f94…`；导出 job `90435dac…`（trace `f5e2c088…`，executor ref `MOCK-e2b3…`）；Benchmark 收藏 `materials:unit_cost:mock-1` 记录 ID `136d36ce…`。Audit Drawer 仍因 `/api/planner/audit` 未实现返回 404，按文档标记为既知限制。 | 将上述 trace/job/收藏 ID 同步到 `go_live_package.md` 执行回执；等前端更新默认 `user_id` 后在 UI 再跑一次确认无 422。 |
| 2025-12-14 | Smoke – 压测记录 | Planner | 重新运行 `python scripts/run_load_test.py` 并比对监控。 | Scenario export：count=500 min=0.063s p50=0.496s p95=0.902s max=2.078s failures=0；Benchmark suggest：count=500 min=0.018s p50=0.153s p95=0.285s max=0.371s failures=0；message_bus 未接入 Kafka，日志持续输出 `message_bus_fallback storing event locally`，已写入审批包风险。 | 待补：Ops 在生产接入真实消息总线并观察 lag；若 fallback 仍触发需报警。 |
| 2025-12-14 | Phase4 验证 | Backend Agent | 按指令执行 `alembic upgrade head`、`pytest tests/planner -q`、`python scripts/validate_env.py --env-file ../.env.sample`、`python scripts/run_load_test.py` 并采集 `/api/planner/audit` 结果。 | ✅ Alembic 连接 PostgreSQLImpl，0004 迁移成功；`scenario_favorites`/`benchmark_favorites`/`planner_executor_callbacks`/`audit_logs` 均含 created_at/updated_at；新增索引 `idx_audit_target_action_created_at`。`pytest` 12 passed；`validate_env` 全绿；`run_load_test`：scenario export count=500 p95=1.105s、benchmark suggest count=500 p95=0.291s（消息总线仍 fallback）。`curl /api/planner/audit` 仍 404（接口尚未发布），已从 `audit_logs` 提供 JSON 样例。 | 待办：如需满足前端/Docs 引用，需在后端补充 `GET /api/planner/audit`（含分页/过滤）并在网关放行；消息总线切换至真实 Kafka 后复测 load test 与审计 API。 |
| 2025-12-14 | feature/costing-backend-phase4 | Backend Agent | 实现 `/api/planner/audit`（分页/过滤/trace_id）、schema、测试、接口说明。 | 新增 `AuditLogRead` + `PaginatedAuditLogResponse`，路由支持 target_type/target_id/action/actor/trace filters & pagination；`test_audit_*.py` 覆盖；准备 curl 示例和响应 JSON 供 Docs/QA；`pytest tests/planner -q` 验证通过。 | 待办：部署到目标环境、放通网关后重新通知前端/QA。 |
| 2025-12-14 | 前端部署 & Smoke | Frontend Agent | `git pull && npm ci && npm run build`，将 `dist/` 部署到 `/var/www/html/ai-costing/dist` 后运行 UI Smoke。 | 场景列表/导出历史/AI 收藏/Job Drawer Smoke 成功：job `ac7c0c44-1de4-4ea9-8a60-2c4c83d1b8f1` (CSV)、`3d0f0d4a-c1d1-4d58-8bfc-6d5d5f1c1c44` (clone)、`4b22a9c1-6d08-4a6b-938a-8cb8d4ee18e4` (export)；AI 收藏 trace `TRACE-LIVE-AI-01` favorite `fav-a0f52c5f-2af7-4b21-b1d9-84de9946a2de`；Job/Audit Drawer 展示“Audit API 即将开放”提示。 | 风险：Audit API 仍 404 → Audit Drawer/导出历史暂不可用；消息总线 fallback 日志存在需持续监控；收藏需在正式 build 上操作，preview 环境不保留状态。 |
| 2025-12-14 | 文档更新 | Docs Agent | 同步 go-live 索引、手册、运行手册，反映最新前端命令/截图/Audit 状态 | `go_live_index.md`、`go_live_package.md`、`go_live_minutes.md` 与 `planner_user_guide.md` 均已更新：记录 Smoke job/trace/favorite ID、附录截图/日志、Audit API 未上线说明。 | 残留风险：Audit API 仍 404、消息总线 fallback 日志持续；待后端正式发布 `/api/planner/audit` 并接入真实 Kafka 后再更新文档。 |
| 2025-12-14 | Phase0 样本规划 | Docs/QA | 在 `requirements.md` 新增“产品模型主线”章节并创建 `phase0_sample_data.md`，沉淀样本来源与验收清单 | 要点涵盖工艺模块、虚拟物料、标准/实际尺寸、变体规则、SKU 绑定与样本验收；Phase0 样本文件链接引用基础表单 PDF/XLSX。 | 待办：按清单导入样本并回填结果；Audit API 未放通仍属风险，需在后续部署落地后更新文档。 |
| 2025-12-14 | 前端部署指引执行 | Frontend Ops | 以 `/srv/www/ai-costing-system/planner` 为静态根运行 `npm run deploy:static`（新增 rsync→cp fallback），随后通过 API 完成 CSV→克隆→审批→导出→Benchmark 收藏 Smoke，并记录 job/favorite ID。 | 静态目录写入成功；CSV 作业 `c92e9070-f0ee-494c-ad27-1e4811e71c79` 6/6 完成；克隆 job `0ee71324-0853-4169-b688-a3c8aab3632e` 生成 `SCN-SMOKE-CLONE`；审批 submit/approve 分别记录 trace `f9b99478-...` / `a90a024e-...`；导出 job `b8b7a6c0-7a73-4ef2-a58d-7ba58779fe39` → executor ref `MOCK-e1a53413...`；Benchmark 收藏 `materials:unit_cost:mock-1` favorite `23022470-e417-4210-9809-343204841bab` 且请求体含 `user_id=planner_user`。Audit Drawer 由于 `/api/planner/audit` 未发布仍显示“后端尚未提供...”提示，经两次 `curl` 复查 404。 | 待办：安装 `rsync` 还原增量同步；补齐 GET `/api/planner/audit`（分页+过滤+重试），上线后再由 UI 截图；若需对外访问静态资源请在 Nginx 加入 `/srv/www/ai-costing-system/planner` location 并配置缓存头。 |
| 2025-12-14 | Phase4 Smoke（Audit API） | QA Agent | 参考 `DOC/costing/images/go-live-20251214/cli-smoke-log-20251214.txt` 重跑 CSV→克隆→审批→导出→AI 收藏→Audit API | 生产网关：clone job `6d6000c6-5e33-40c7-b21c-d77ddf14fa12`、export job `52def228-19c7-457c-9b16-3b793b9b9ca8`、favorite `0b3a69d9-398c-463a-b35a-5f0752e3667b`，但 `/api/planner/audit` 仍 404；本地节点（127.0.0.1）已返回 submit/approve/export 记录（trace `305ba462-...`, `10a14a7f-...`, `429bb8d5-...`，job `0641fb72-a9ff-49df-8bb2-39feb7dc675c`, scenario `c5772118-684e-4d3a-a6e2-57de7939fb64`）。 | 待办：部署带 Audit Router 的 backend 至公网并放通，再由 QA/前端确认 Drawer 恢复；期间持续监控消息总线 fallback。 |
| 2025-12-14 | Smoke – Planner API (8800) | Backend Ops | 以 CLI 方式在 8800 环境再次跑通克隆→审批→导出→AI 收藏，输出提供给前端验证。 | ✅ baseline `SC-LOAD`（90ae42f9-b74f-4ac9-9933-c64c92db2bb6）查询成功；克隆 job `6d6000c6-5e33-40c7-b21c-d77ddf14fa12` 生成 `bd57e323-d2ae-4e2f-af83-66ccecd6bc41`; submit/approve API 正常；导出 job `52def228-19c7-457c-9b16-3b793b9b9ca8` 返回 `MOCK-bd57e323...`; Benchmark 收藏 favorite `0b3a69d9-398c-463a-b35a-5f0752e3667b`; `GET /api/planner/audit` 仍 404。全部命令及响应记录于 `DOC/costing/images/go-live-20251214/cli-smoke-log-20251214.txt`。 | 待办：前端依据上述 ID 在 UI 捕获截图；后端需确认为何 8800 实例尚未升级到含 `/api/planner/audit` 版本，必要时重新部署。 |
| 2025-12-14 | Audit API 上线 | Backend Ops | 拉起最新 backend 并重启 `planner-costing.service`，确认 `/api/planner/audit` 可用。 | `systemctl --user restart planner-costing.service` 成功；`curl -G http://127.0.0.1:8800/api/planner/audit?target_type=scenario&target_id=bd57e323-...` 返回 200，total=3，actions=submit/approve/scenario_export；记录已追加到 `cli-smoke-log-20251214.txt`，`go_live_package.md` Planner UI Smoke 表更新为 ✅。 | 待办：前端按相同 ID 补齐 Audit Drawer 截图；若需要系统级 `planner-api.service` 也更新，请提供 sudo 访问或由 Ops 执行。 |
| 2025-12-14 | UI Smoke 支援 | Backend Ops | 重新启动 `planner-costing.service`，用 CLI 跑一遍 clone→submit→approve→export→AI 收藏链路，并把新的 job/trace/favorite ID 追加到 `cli-smoke-log-20251214.txt`，供前端按相同 ID 截图。 | ✅ baseline `SC-LOAD` 仍可查询；新建 `SCN-SMOKE-CLONE-UI`（job `79c47946-87e5-42c6-82cf-4136f9aaa469`，scenario `c5772118-684e-4d3a-a6e2-57de7939fb64`）；提交/审批成功；导出 job `0641fb72-a9ff-49df-8bb2-39feb7dc675c`（executor ref `MOCK-c5772118-...`）；AI 收藏 `3cfbf935-3605-4b93-bff0-45025990e7e9`；`/api/planner/audit` 返回 submit/approve/scenario_export 3 条；全部响应写入 CLI log。 | 待办：需要 GUI 端按上述 ID 截图（toast、Job Drawer、Network、Audit Drawer）并更新 `go_live_package.md` 证据列；Docs 需回填截图文件名。 |
| 2025-12-14 | Phase4 Go-live Summary | Planner | 汇总 Backend/Frontend/Docs/QA 最新结果，形成 Phase4 go-live 总结并指向所有证据 | 新增 `DOC/costing/go_live_summary_phase4.md`，涵盖交付范围、验证结果、当前状态、残余风险与下一步；引用 `cli-smoke-log-20251214.txt`、`go_live_package.md`、Prometheus 指标等 | 等 QA 完成最新复测后，在 summary 与 `qa/phase4_regression.md` 中补充结果；若 Ops 放通 Audit API，需更新 summary residual risks |
| 2025-12-14 | 主项目执行方案 | Planner | 根据《讨论要点总结》《终极开发指南》整理主项目可交付方案，明确阶段、任务、分支与验收标准 | 新增 `DOC/costing/main_project_plan.md`：Phase0–Phase5 路线、Workstream 职责、分支命名、依赖配置、风险缓解与下一步行动 | 需要各角色确认阶段时间/负责人，并据此创建 Phase0 分支、准备样本数据与验收条目 |
| 2025-12-14 | Go-live 证据 | Planner | 将总工上传的 UI Smoke 截图与 CLI 日志统一归档，并在 `go_live_package.md` 更新对应文件名 | `DOC/costing/images/go-live-20251214/` 现包含 `wechat_2025-12-14_*.png`、`01-approval-list.png.png`、`cli-smoke-log-20251214.txt`，表格证据列已引用实际路径，方便审批核查 | 如有新增截图/录屏或替换版本，需同步更新该目录与 `go_live_package.md` |
| 2025-12-14 | Go-live 监控 | Backend Ops | systemd `planner-api.service` 加入 `PYTHONPATH` 并重启；持续监控 `/api/planner/audit` 与 `/metrics`（消息总线/执行器指标）；`git pull --ff-only` 校验代码同步，CLI 日志追加最新结果。 | ✅ `planner-api.service` 处于 active 状态，系统级 `curl /api/planner/audit` 返回 200（总数 535）；`/metrics` 中 `planner_message_bus_events_total` 与 `planner_executor_requests_total` 正常递增；`cli-smoke-log-20251214.txt` 新增 15:36/15:45 记录；仓库已与 origin 同步。 | 持续观测 systemd 服务与指标；若需对公网放通 Audit API，配合 Ops 更新网关/防火墙并记录命令；消息总线若切换 Kafka，需再跑一次 `/metrics` 校验 |
| 2025-12-14 | Audit API 暂不开放公网 | Backend Ops | 评估对外放通风险：`/api/planner/audit` 返回包含 trace_id、actor_id、payload 等敏感信息，目前尚未完成 WAF/ACL 及访问审计审批，因此保持仅内网（VPN）可访问。 | ✅ 决议：暂不放通公网，待安全审批与网关策略 ready 后再执行；任务记录与原因已写入 task log，CLI 监控继续保留在 8800 内网。 | 待办：与安全/Ops 对齐放通前置条件（IP 白名单、鉴 权、速率限制、审计日志镜像）；审批完成后再补网关/防火墙命令并更新 log |
| 2025-12-14 | Phase1 数据准备 | Planner | 梳理宜搭物料/工序表单字段、尺寸换算与变体规则，并更新 `yida_sync_integration.md`。 | 新增第 7 章：列出物料/人工字段、必填性、落地字段、同步过滤、责任人及尺寸/虚拟物料/变体规则策略；明确 `FORM-0X966971PURFOJA97LNHJBBLKUK1234Z6COOLZ` 为物料来源。 | 待办：Planner/ops 登陆宜搭填写控件 ID；Backend 建 `yida_materials.json` & 同步脚本；Docs 将同步流程纳入 go-live runbook。 |
| 2025-12-14 | feature/costing-backend-phase0 | Backend Agent | 引入 `MaterialSyncJob` 表（迁移 `0006_yida_sync_jobs`）并将 `/api/planner/base-config/materials/sync-yida` 改为异步 Job（BackgroundTasks + `run_material_sync_job`）。 | ✅ Alembic 升级成功；API 现在返回 `MaterialSyncJobRead`，可通过 `/materials/sync-jobs/{id}` 查询进度，脚本/cron 将统一落库。 | 待办：结合 systemd timer 批量触发，并输出成功/失败日志到 Prometheus/告警渠道。 |
| 2025-12-14 | feature/costing-backend-phase0 | Backend Agent | 新增 YiDa 工序/人工同步：配置 `yida_processes.json`+mock data、`ProcessSyncService`、API `POST /base-config/processes/sync-yida`、脚本 `scripts/sync_yida_processes.py`。 | ✅ dry-run/正式同步均返回 `{total_fetched:2, processes_created:2, modules_created:2, models_created:1, model_links_created:2}`，成功写入 `processes`/`process_modules`/`model_processes`；缺失模型会自动建档。 | 待办：替换真实 `form_uuid`/字段 ID，接入定时任务与失败告警。 |
| 2025-12-14 | backend-material-endpoints | Backend Agent | 实装材料管理 API：`GET /base-config/materials`（分页/过滤）、`PATCH /materials/{id}`（启用/禁用）、`POST /materials/export`（CSV/XLSX），以及 `GET /materials/sync-jobs` & `/sync-jobs/{id}`。 | ✅ 新增 `material_service`、`MaterialRead`/`MaterialExportRequest` schema、openpyxl 依赖；导出支持 CSV/Excel；FastAPI 路由与单测 `test_material_endpoints.py`（覆盖列表/更新/导出/同步日志）全部通过，`pytest tests/planner -q` 21 passed。 | 待办：无（前端已可对接）。 |
| 2025-12-14 | feature/costing-frontend-phase0 | Frontend Agent | 物料主数据管理界面（列表筛选/启用状态、导出、同步日志）。 | ✅ 新增 `/costing/materials` 路由与侧边导航入口；集成 React Query 与 `GET/PATCH/POST /base-config/materials*`、同步日志 Drawer、导出按钮、YiDa 手动同步确认；`npm run test -- --run`、`npm run build` 均通过，README 与 task log 更新。 | 待办：等待真实数据后补充截图及用户手册章节；如需要进阶筛选（分类、供应商）则在后续迭代扩展。 |
| 2025-12-14 | feature/costing-backend-phase0 | Backend Agent | 材料管理 API 实装：`GET /base-config/materials`（分页/过滤）、`PATCH /materials/{id}`（启用/禁用）、`POST /materials/export`（CSV/XLSX）、`GET /materials/sync-jobs`。 | ✅ 新增 `MaterialFilters` service、openpyxl 依赖、导出 helper；前端所需接口全部可用，`pytest tests/planner/test_materials.py -q` 4 通过。 | 待办：README/schema 说明 material_type/material_ref_id（todo3）。 |
| 2025-12-14 | 宜搭物料同步 | Backend Ops | dry-run：`python scripts/sync_yida_materials.py --dry-run --config yida_materials.json`；正式：`systemctl start yida-material-sync.timer`（调用 `/base-config/materials/sync-yida`，后台创建 `MaterialSyncJob`）。 | ✅ dry-run 报告：`total_records=5, to_create=5, to_update=0`；正式作业 `job=0b42bdf2-54f1-4f69-8ba3-36655ad8e2ee` 状态 completed，`GET /materials/sync-jobs/0b42bdf2...` 返回详情；依赖 `requests`, `openpyxl`, `python-dotenv`。 | 定时器 `yida-material-sync.timer` 计划每日 04:00 运行；若失败需查看 `logs/material_sync/*.log` 并重跑；仍待替换真实 YiDa form UUID & API Key。 |
| 2025-12-15 | feature/costing-backend-phase0 | Backend Agent | 物料图片通过钉钉开放平台下载：新增 `images` 字段代理、`GET /base-config/materials/{id}/images/{idx}` 路由、`DINGTALK_APP_KEY/SECRET` 配置、`dingtalk_client`、YiDa 同步写入图片元数据。 | ✅ `MaterialRead.images` 返回可直接访问的 API 路径；新路由在无凭证时返回 503 提示；`scripts/sync_yida_materials.py` & `/materials` 列表均含图片数据；`pytest tests/planner -q` 22 passed。 | 待办：在生产环境配置钉钉 AppKey/Secret，并重启服务后让前端走代理地址；如需直接返回公网 URL，可在后续扩展临时签名/缓存。 |
| 2025-12-15 | feature/costing-backend-phase0 | Backend Agent | 增加物料单位换算字段 `conversion_purchase_to_bom` / `conversion_bom_to_inventory`，支持 API 显示与 PATCH 更新，并同步 Alembic 迁移 `0008_material_conversion_columns`。 | ✅ 列表/详情返回字符串形式的换算值；若未配置则默认为 1.0；`MaterialUpdateRequest` 可更新并立即生效；`pytest tests/planner -q` 22 passed；`alembic upgrade head` 成功。 | 待办：与前端对齐字段显示逻辑；如需从宜搭导入换算说明，可在 metadata 中读取 `textField_m3pgyx4d`。 |
| 2025-12-15 | feature/costing-backend-phase0 | Backend Agent | 确认可用的钉钉附件下载方式：验证宜搭仅返回 `/ossFileHandle`，通过 `v1.0/yida/apps/temporaryUrls/{appType}` + `fileUrl` 才能得到真实 OSS URL。 | ✅ `curl`/脚本复现：`gettoken` 后携带 `fileUrl/systemToken/userId` 可得到临时签名 URL，丢 `fileKeys` 则返回 `MissingfileUrl`；README 已说明代理流程和配置项。 | 需在 dingtalk_client 中统一封装 temporaryUrls 调用，并在生产配置正确的 `appType/systemToken/userId`。 |
| 2025-12-15 | feature/costing-backend-phase0 | Backend Agent | 实现虚拟物料服务：REST API（列表、详情、创建/更新、批量绑定、停用、真实物料引用查询）+ 盘点折算接口。 | ✅ 新增 `VirtualMaterialService`、`/virtual-materials` 全套接口、绑定仅允许非 BOM 真实物料且自动维护 `materials.metadata.virtual_links`；`inventory-breakdown` 可直接换算真实物料消耗；`test_virtual_materials.py` 覆盖主要路径，`pytest tests/planner -q` 23 通过。 | 待与前端联调虚拟物料管理界面，并在 docs/uSpecs 中补充 UI 描述。 |
| 2025-12-15 | feature/costing-frontend-phase0 | Frontend Agent | 物料主数据成本参数抽屉改造：本地 BOM 标记、单位换算表单、推算库存单价、宜搭只读字段。 | ✅ `MaterialMasterPage.tsx` 新增采购→BOM / BOM→库存校验、BOM 单价实时推算、列表“库存单价”计算列、BOM 开关和库存单位本地保存，抽屉底部提示文案与“采购规格/成本公式”只读展示同步上线；`npm run test -- --run`、`npm run build`、`npm run deploy:static` 均通过并已发布至 `/var/www/html/ai-costing/dist`。 | 待办：若后端暴露更多只读标记（如 `inventory_unit_from_yida`）需继续同步 UI 只读状态，关注用户反馈以决定是否开放 BOM 单价编辑。 |
| 2025-12-15 | feature/costing-backend | Backend Agent | 钉钉图片多图&缓存支持：临时 URL 代理 + 本地缓存。 | ✅ 更新 `DingTalkClient` 解析 `/ossFileHandle` 与 `/fileHandle` 全量图片，调用 `v1.0/yida/apps/temporaryUrls` 并落盘缓存（`DINGTALK_CACHE_DIR`/TTL 参数）；`MaterialRead.images` 永远生成 `/api/planner/...` 地址；`/base-config/materials/{id}/images/{idx}` 遍历多图并先查本地缓存，命中则直出，未命中再拉钉钉。`pytest tests/planner/test_material_endpoints.py` 通过，`planner-costing.service` 已重启验证。 | 可选：在其他节点布署时记得配置 `DINGTALK_*` & 缓存目录；如需清理缓存可定期清空 `backend/.cache/dingtalk`。 |
| 2025-12-15 | feature/costing-frontend-phase0 | Frontend Agent | 物料列表增强：宜搭创建时间列、BOM 单位中文、默认筛选。 | ✅ MaterialMaster 列表显示 `source_created_at`（取自宜搭）、BOM 单位选项/渲染改为“平米/米/个”，筛选区域默认勾选“仅启用 / 仅 BOM 物料”；Smoke 时可直接看到新增列与默认筛选行为；`npm run test -- --run`、`npm run build`、`npm run deploy:static` 已执行。 | 待办：若后续需要更多宜搭字段（例如同步人、更新时间）再补列；同时关注用户对默认筛选的反馈。 |
| 2025-12-15 | feature/costing-base-config | Backend/Frontend | 物料详情抽屉保存需同时落库 `bom_unit_price`，供库存单价计算与稽核；后端补充 schema/metadata，前端保存逻辑按换算自动推算该值。 | ✅ Alembic `0008` 延续、`MaterialUpdateRequest`/router/test 支持 `bom_unit_price`，写入 `metadata_json`; 前端 `MaterialMasterPage` 依据入库单价与换算推算并在 PATCH 时一并发送；README、task log 更新。`pytest tests/planner/test_material_endpoints.py`、`npm run test -- --run`、`npm run build` 均通过。 | 待办：如需允许手动覆盖 BOM 单价，再评估 UI 输入项与权限；同步 Docs 说明“库存单价仅供核对”。 |
| 2025-12-15 | feature/costing-base-config | Backend/Frontend | 物料“分类”筛选项沉淀为脚本+常量文件，供多处 UI 复用，并修复 API 分类过滤命中宜搭 `textField_jacd537`。 | ✅ 新增 `backend/scripts/export_material_categories.py`（输出 `frontend/src/constants/materialCategories.ts`），README 增加执行说明；MaterialMasterPage 先加载常量再合并 API/当前页分类；`apply_material_filters` 兼容 PostgreSQL/SQLite JSON 提取并补单测；`npm run test -- --run`、`npm run build`、`pytest tests/planner/test_material_endpoints.py` 通过。 | 后续若分类有增量，只需运行脚本或在同步后重跑；如要对外提供枚举，可在 API 增加 `GET /materials/categories`。 |
| 2025-12-15 | Ops/Backend | planner-api systemd 服务报 `ModuleNotFoundError: src.api`，无法重启 | ✅ 调整 `/etc/systemd/system/planner-api.service` 的 `ExecStart`（`/usr/bin/env PYTHONPATH=... uvicorn ...`）并执行 `daemon-reload && reset-failed && start`，service 状态恢复 `active (running)`；前端刷新后分类筛选生效 | 记录重启步骤于 runbook；若后续再调整 PYTHONPATH/虚拟环境，记得同步 systemd 配置后 `daemon-reload` |
| 2025-12-15 | Docs 更新 | Docs Agent | 汇总物料单位换算、缩略图、分类常量、宜搭同步字段的文档（`planner_user_guide.md`、`go_live_package.md`、`go_live_index.md`、`go_live_summary_phase4.md`、`requirements.md`、`phase0_sample_data.md`、`runbooks/yida_sync_scheduler.md`、`main_project_plan.md`） | ✅ 新增“物料管理”章节、Go-live 包/summary 记录成本参数抽屉 & 列变化、runbook/mpp/requirements 补充宜搭只读字段 vs 本地字段及 `export_material_categories.py` 使用说明，Phase0 文档追加字段映射与 Phase1 对账脚本。 | 无（待 Audit API 放通后再更新 Known Issues）。 |
| 2025-12-16 | 基础配置（物料/虚拟物料） | Backend & Frontend | 为真实/虚拟物料新增“计算方式”字段（面积/周长/数量/宽度/高度）并与 BOM 单位联动，同时排查“新建虚拟物料无法保存”——补充表单校验与错误提示。 | ✅ Alembic `0009_material_calculation_method` 新增列；Material/Virt API + schema + README/requirements/uispec 更新；MaterialMaster/VirtualMaterials 页面加入下拉、自动同步单位、列表/详情列展示；Form 保存失败会提示后端错误。 | 若需从宜搭同步 `calculation_method`，后续在 `yida_materials.json` 增列并更新同步脚本。 |
| 2025-12-16 | 虚拟物料折算 | Backend Agent | 调整虚拟物料模型及盘点 API：去掉对 `calculation_method` 的依赖、放宽绑定校验允许 BOM 物料、让调用方在盘点请求中传入 `calculation_method/usage_context` 并仅按配比展开。 | ✅ `0010_drop_virtual_material_calc_method` 迁移移除列；FastAPI schema/service/router 移除字段并在 `/virtual-materials/{id}/inventory-breakdown` 新增 `calculation_method` / `usage_context` 入参与回显；真实物料 `metadata.virtual_links` 继续提示引用；单测 `pytest tests/planner/test_virtual_materials.py -q` 更新并通过，README & task log 同步说明。 | 前端待适配（盘点请求需传 context）；如需再次暴露计算方式，可在 metadata 中自定义。 |
| 2025-12-16 | 部署支持 | Backend Ops | 线上接口 404→500：需切换 admin 账号执行 `alembic upgrade head`、重启 user-scope `planner-costing.service`。 | ✅ 在服务器上 `su - admin` → `.venv/bin/alembic upgrade head` → `sudo -u admin XDG_RUNTIME_DIR=/run/user/1000 systemctl --user restart planner-costing.service`，`curl /api/planner/base-config/virtual-materials` 返回 200。 | 后续如再部署 backend，记得用 admin 身份执行 upgrade + user service restart。 |
| 2025-12-16 | 虚拟/真实物料管理 | Backend & Frontend | 1）修复虚拟物料绑定方式保存后被重置的问题；2）在物料成本参数 Tab 与虚拟物料绑定弹窗的“物料详情”抽屉之间打通“本地描述”字段，并统一放置在成本参数 Tab 底部。 | ✅ 后端 `/virtual-materials/{id}/bindings` 读写 `binding_type`（ratio/quantity）并通过 `metadata_json` 落库，重启 `planner-costing.service` 验证接口返回正确；前端 VirtualMaterialsPage 保留绑定表单状态；MaterialMasterPage/MaterialDrawer 共享 `metadata_json.local_description` 文本框并实时推算 BOM 单价。`npm run test -- --run`、`npm run build`、`pytest tests/planner/test_virtual_materials.py` 均通过且静态资源已重新部署。 | 继续观察用户在 UI 上切换绑定方式与描述同步是否稳定，如需更多字段互通再扩展 metadata。 |
| 2025-12-16 | 虚拟物料分类/默认单位 | Backend & Frontend | 虚拟物料需要与真实物料共用分类，并将单位固定为“套”。创建面板需要分类下拉，列表新增分类列。 | ✅ Alembic `0011_virtual_material_category_unit_default` 为 virtual_materials 增加 `category` 列、将 unit 默认值设为“套”，并顺便把 `alembic_version.version_num` 扩大为 VARCHAR(64) 以支持更长的 revision id；API/schema/service/test 均已更新并支持按分类过滤。前端 VirtualMaterialsPage 在筛选区、列表、详情表单中加入分类下拉（数据源=materialCategories 常量 + 当前列表/详情补充），表单默认单位提示改为“系统固定=套”，`npm run test -- --run`、`npm run build`、`deploy_static.sh` 全部通过。 | 后续如需动态维护分类枚举，可在后台增加 `GET /materials/categories` 并在前端改为实时加载；请在其它环境也运行 `alembic upgrade head` 以获得新列和版本表长度调整。 |
| 2025-12-16 | 工艺模块落库 | Backend Agent | 落库 `process_module_materials`/`process_module_steps`，实现工艺模块 CRUD/启停/复制/预览/引用查询，保存/复制保留宜搭字段并写审计日志，允许引用虚拟物料。 | ✅ 迁移 `0011_process_module_components` 创建子表；新增 `process_module_service` 与 `routers/process_modules.py`（`GET/POST/PATCH/activate/deactivate/copy/references`）；README+schema+docs 更新说明；新增单测 `pytest tests/planner/test_process_modules.py -q` 覆盖物料/工序校验、启停、复制、引用列表。 | 前端可据此开发工艺模块管理与产品模型引用；若需质检/附件等字段，可通过 `metadata_json` 延伸。 |
| 2025-12-16 | 工序主数据 & API | Backend Agent | 扩展 `processes` 表并打通工序 CRUD/启停/复制/引用 API，允许工艺模块步骤引用全局工序。 | ✅ 迁移 `0013_process_master` 添加新字段及 `process_module_steps.process_id` 外键；新增 `process_service.py`、`routers/processes.py`、Pydantic schema；列表/创建/更新/启停/批量启停/复制/引用接口均写入审计日志；`pytest tests/planner/test_processes.py -q` & `pytest tests/planner -q` 全绿。 | **纠偏**：工序库应保持“通用字典”，班组/工价/工时/计量等调参应落在工艺模块步骤行（见 `DOC/基础表单/谌婷婷发起的工艺列表_1765587959158.pdf`）。前端/文档需按此口径收敛字段。 |
| 2025-12-16 | 工序计价规则 & UI 优化 | Planner + Frontend Agent | 明确两类计价成本公式与两个边界规则（历史工价、同工序多班组），并据此优化工艺模块步骤调参与预览计算。 | ✅ 计价规则：计时成本 = (基础工时 + 单位工时 × 计价量[㎡/m/个]) × 分钟单价；计件成本 = 计件单价 × 订单数量。边界规则：1）工价调整后历史订单核算必须“成本快照”（订单/报价层落库人工成本明细，保留当时参数与金额）；2）同工序不同班组不同价：优先在**工艺模块步骤行选择班组并配置工价/工时**；若业务坚持“同工序不同价”为不同标准，可在工序库中用不同工序编码区分（如“下料-铝合金/下料-实木”）。 | ✅ 前端落地：工艺模块 `ProcessModulesPage.tsx` 支持在工序组步骤行写入计价参数（metadata_json），并提供预览计算（订单输入 → 每步成本 + 合计）。**纠偏**：工序库不再作为班组/工价/工时的配置入口。 |
| 2025-12-17 | 工艺模块文档更新 | Docs Agent | 将“工艺模块 = 物料组 + 工序组 + 虚拟物料引用”及版本/发布机制写入 `requirements.md`、`ui_specs/process_modules.md`、`planner_user_guide.md`、`main_project_plan.md`，并在文档中引用宜搭样例 PDF；同步 Task Log 记录。 | ✅ 新增 `requirements.md` §13（模块组成、版本流程、虚拟引用口径）、`ui_specs/process_modules.md`（列表/编辑器/版本历史/接口规范）、`planner_user_guide.md` §9（操作流程、复用原则、虚拟引用提示）、`main_project_plan.md` Phase2 更新（模块结构、宜搭样例引用、版本化说明）；所有章节均引用 `DOC/基础表单/产品模型_1765543113804.pdf` 作为字段映射样例。 | 待办：待前端交付模块管理 UI 后补充截图；若宜搭模板更新，需同步替换 PDF 链接及字段映射表。 |
| 2025-12-17 | 工序库列表样式统一 | Frontend Agent | `/costing/processes` 列表样式需要与 `/costing/virtual-materials` 保持一致（标题+说明→筛选卡片→列表卡片），并修复“分类/班组挤一团”以及列表底部多出的灰色横向滚动条。 | ✅ `ProcessesPage.tsx` 改为与虚拟物料页一致的三段布局；表格使用 `tableLayout="fixed"` + `ellipsis` 并开启横向滚动（同时通过 `.hide-x-scrollbar` 隐藏灰色横向滚动条）；按用户反馈把“分类/班组”列宽调整为更窄（约 120/180）避免视觉拥挤。 | 已执行 `npm run build` 并用 `PLANNER_STATIC_DIR=/var/www/html/ai-costing/dist ./scripts/deploy_static.sh` 发布，用户确认生效。 |
| 2025-12-17 | 缩略图恢复 & 图片永久落盘 | Backend Ops + Backend Agent | `/costing/materials` 与虚拟物料绑定区缩略图全部不显示（图片接口 503）。用户要求“同步时永久落盘（方案B）”，避免钉钉临时链接/凭证问题导致后续再次不可见。 | ✅ 定位根因：`GET /api/planner/base-config/materials/{id}/images/{idx}` 依赖钉钉凭证，缺失时返回 503（`DingTalk credentials are not configured`）。✅ 修复：`DingTalkClient` 在 env 缺失时回退读取 `backend/config/yida_materials.json` 的 `app_key/app_secret`。✅ 永久落盘：新增本地媒体存储（默认 `backend/media`），图片接口优先读 `metadata_json.local_images` 本地文件，不存在则从钉钉拉取并**落盘**，同时同步流程 `sync-yida` 也会在写入 `images` 时下载并落盘。 | 新增 env：`PLANNER_MEDIA_DIR`、`PLANNER_PERSIST_MATERIAL_IMAGES`（默认 true）。已重启 8800 后端并验证接口经 nginx 返回 200（`Content-Type: image/png`），缩略图恢复。 |
| 2025-12-17 | 工艺模块工序行一行编辑 | Planner → Frontend Agent | 用户要求“新建工艺模块”工序组表格单行展示并编辑全部计时/计件参数；工序库仅为字典不含班组/工价/工时等值。 | ✅ 已形成前端实施规范与验收用例，见 `DOC/agents/briefings/frontend_process_module_step_row.md`。 | Frontend：按 briefing 完成表格列、联动禁用、保存回显验收；并更新 UI 文案避免“工序库带参数”的误导。 |
| 2025-12-17 | 虚拟物料规则升级 | Backend & Frontend | 用户反馈“虚拟物料单位/规则不清导致工艺模块引用有问题”，要求区分配方型(按比例)与套件型(按数量)。 | ✅ 增加 `virtual_kind`：`recipe`/`kit`（落在 `metadata_json.virtual_kind` 并在 API schema 中显式返回）；后端强校验：recipe 仅允许 ratio、子物料单位一致且配比合计=1；kit 仅允许 quantity、单位固定“套”、每套数量可小数；真实物料 `metadata_json.virtual_links` 回写 `virtual_kind/binding_type`。前端 VirtualMaterialsPage 增加“类型/单位”编辑与绑定表格联动（recipe/kit 不再混用），套件型允许不同单位并正确汇总“每套成本”。 | 后续：如需“单位可换算成同一单位”而非字符串一致，需要引入单位换算字典/映射表；并在产品模型层实现按面积/周长等驱动的虚拟展开。 |
| 2025-12-17 | 占位型虚拟物料（模板化工艺） | Docs Agent | 工艺流程通用但物料随 SKU 变化，导致需要创建大量重复工艺模块；决定在“虚拟物料”新增占位型 `virtual_kind=placeholder`，工艺模块仅引用虚拟物料，替换/变体判断在产品模型层完成。 | 需要在文档中统一口径：1）占位型字段：`placeholder_symbol`（自动生成 `#{...}`）、`constraint_category`、`unit` 必选、不可绑定子物料、BOM 单价=0；2）工艺模块引用占位型不做校验；3）产品模型启用校验：若存在占位符但未配置替换映射→禁止启用（草稿可保存）。 | **待办**：更新 `DOC/costing/ui_specs/virtual_materials.md`（新增占位型章节与交互）、更新 `DOC/costing/requirements.md`（模型启用校验与替换层级）、并在本 `task_log` 记录前后端实现完成情况与截图/验收点。 |
| 2025-12-17 | 虚拟物料类型回显修复（兼容旧后端） | Frontend Agent | 用户反馈：点“配方型”保存成功，但重新打开又变回“套件型”。 | ✅ 定位：当前线上后端（`47.99.89.206:8800`）返回结构中不含 `virtual_kind`（OpenAPI 也未暴露），导致前端回显只能走默认 kit。✅ 修复：前端改为“绑定区按钮切换类型（配方型/套件型）”，隐藏“单位”输入；“保存绑定”时会先 PATCH 落库 `virtual_kind + unit`（kit 强制“套”；recipe 自动从子物料单位推导），再 PUT 保存 bindings 并强制 `binding_type`（recipe=ratio、kit=quantity）。✅ 兼容：若后端仍不返回/不保留 `virtual_kind`，前端通过 `unit != 套` 或 `bindings[].binding_type` 推断类型以保证回显稳定。 | 待办：后端 8800 需升级到包含 `virtual_kind` schema/service 的版本后，再移除推断兼容逻辑并以 API 字段为准；同时可补充“单位换算表”来替代字符串一致校验。 |
| 2025-12-17 | 虚拟物料占位型（placeholder） | Backend & Frontend | 新增第三种类型占位型，用于工艺模块模板化；要求“任何引用处一眼识别占位”，并支持编辑时自动去掉 `#{}`、保存再加回。 | ✅ 前端：新增占位型入口、必填单位/约束分类；列表名称对占位型仅展示 `#{名称}`；保存时自动生成 `placeholder_symbol=#{名称}` 并写入 `metadata_json`。✅ 后端：扩展 `virtual_kind` 支持 `placeholder`（schema+service）；占位型创建/更新时强制将 DB `name` 存为 `#{...}`，并同步写 `metadata_json.virtual_kind/placeholder_name/placeholder_symbol`；禁止 placeholder 绑定子物料（bindings 非空返回 400）。✅ 编辑体验：打开抽屉时将 `#{...}` 自动显示为“去符号名称”，保存再落回 `#{...}`。 | **待办（关键）**：后端变更需部署并重启 8800 服务后才会生效（否则“落库即 `#{...}`”只在前端显示层生效）；旧数据可通过“编辑→保存”触发后端规范化。 |
| 2025-12-18 | 产品模型（价格治理） | Backend & Frontend | 用户反馈：工艺模块能带出虚拟物料 BOM 单价/单位，但产品模型清单里显示 `-` 且无法随主数据改价更新。 | ✅ 后端：`POST /product-models/{id}/preview` 现在支持虚拟物料计价（recipe/kit 按绑定真实物料 BOM 单价汇总；placeholder=0）；并新增 `POST /product-models/{id}/refresh-material-prices` 刷新模型清单行 `metadata_json.bom_unit_price/bom_unit` 快照（不改用量/工时）。✅ 前端：`/costing/models` 顶部新增“同步物料价格”按钮；BOM 单价/单位列支持 preview 优先、快照兜底显示。 | 后续：若要历史核算不随改价波动，应在订单/报价层落成本快照（版本化）；并可补充权限/审批（谁有权同步价格）。 |
| 2025-12-19 | 产品模型（UX 收口） | Frontend Agent | 按优先级补齐 `/costing/models`：打样尺寸面板汇总联动、fixed 计算模式联动、占位符映射启用前提示、同步 keep_overrides 开关、清单可读性收口。 | ✅ workspace 顶部宽/高/数量变更后派生面积/周长（订单口径含数量）并 debounce 自动刷新汇总；预览/同步/汇总失败仅 warning 不阻塞。✅ `calc_mode=fixed` 联动 fixed_price 显示/必填，切换非 fixed 会清空且保存时显式置空避免后端保留旧值。✅ 占位符启用前做前端校验（缺映射/单位或分类不匹配/映射到占位型虚拟物料），并引导到“占位符映射”Tab；启用前会尽力落库 placeholder_mappings 以减少“忘点保存”。✅ 从模块同步增加“保留调参”开关（keep_overrides），关闭时二次确认。✅ 工序组计价参数折叠到弹窗，主表更可读。 | 待办：若后端将启用校验错误 detail 结构化输出（而非纯文本），可进一步精确定位到占位符行并高亮。 |
| 2025-12-19 | 产品模型（占位符兜底落库 & 预览/启用口径） | Backend & Frontend | 用户确认：占位符是“占位型虚拟物料”，仅用于工艺模块模板化；进入产品模型清单时必须替换为兜底物料（真实/非占位虚拟），否则无法算价且无法做后续变体兜底。 | ✅ 前端：保存清单（PUT /product-models/{id}/lines）前自动将命中的占位符行替换为兜底物料行，并写入 `metadata_json.origin_placeholder` 追溯；缺映射则阻止保存并引导去“占位符映射”。✅ 后端：当模型清单存在时，预览计算遇到 placeholder 会按 `placeholder_mappings` 解析计价（不再默认 0）；启用校验以模型清单为准并禁止 placeholder 留存；`/product-models/{id}/materials` 优先从模型清单取兜底物料集合；单位比较引入归一化避免 `m/米` 误报。 | 下一步：版本管理/打样版本 vs 标准版本的表结构与发布接口需要单独迭代（见 ui_specs 更新）。 |
| 2025-12-17 | 基础配置（自动编码） | Backend Agent | 统一实现编码号段：新增 `code_counters` 表、`code_generator_service.allocate_code`、`/api/planner/codes/next` 路由，供虚拟物料/工艺模块/工序等调用自动生成编码。 | ✅ Alembic `0014_code_counters` 创建中央号段表；FastAPI schemas + router `codes` 暴露 `POST /codes/next`，基于行级锁保证并发唯一；`router.py` 已 include，README 追加 Codes API 说明。 | 待办：前端/批量脚本接入该接口，并在部署到 47.99.89.206 后复核 `curl /api/planner/codes/next` 调用结果。 |
| 2025-12-19 | 产品模型（版本管理 & SKU 绑定） | Backend & Frontend | 按计划落地版本维度：打样/标准版本、版本清单、发布标准版本、SKU→version 绑定，以及按 SKU 解析尺寸后预览。 | ✅ Backend：新增版本相关表（versions/lines/sku_version_mapping）与 API（versions CRUD、version lines、version preview、publish、sku binding、sku-preview）；占位符仍只存在模块层、版本清单禁止 placeholder；SKU 绑定会校验 model_code 前缀并把解析结果写入 metadata。✅ Frontend：/costing/models 抽屉调整为 5 Tabs（基础信息/工艺路线/标准模型/预览计算/版本管理），引入版本选择器；预览支持“按 SKU 优先”。✅ 兼容：保留旧 `/product-models/{id}/lines/preview` 口径以便过渡。 | 待办：后续把“工艺路线（模块绑定）”也切到 version 维度（目前仍以主档 modules 为源），并补充 SKU 变体映射 UI（variant tokens → placeholder/替换规则）。 |
| 2025-12-19 | 产品模型（打样→标准推导：高级公式模板） | Backend & Frontend | 选择“结构化模板（JSON）+ 从打样校准”的方式，一键将 sample 版本推导生成 standard(1×1) 版本，用于后续 SKU 绑定/库存快照。 | ✅ Backend：新增 `derive_standard_version` 推导服务与接口 `POST /api/planner/product-model-versions/{source_version_id}/derive-standard`，强制 standard spec=1000×1000×1；模板从 sample 行 `metadata_json.derive_template` 读取，输出写入标准版本清单的 `base_quantity + metadata_json.fixed_quantity/coverage_ratio`（工序写入 `metadata_json.base_minutes/unit_minutes`），并记录 `derived_from_version_id/derived_at` 追溯；新增单测 `test_derive_standard_version.py`。✅ Frontend：在“标准模型”清单行增加“高级公式”入口（编辑 derive_template），在“版本管理”增加“一键推导标准版本”面板（选择源 sample、目标模式、新建/覆盖），推导成功后自动切换到标准版本继续保存/发布。 | 待办：补充更多模板类型（分段/最小起步/封顶更精细策略）、以及“版本级变体规则”UI（当前仍以主档规则为主）。 |
| 2025-12-22 | 发货对账（页面增强：导入上传） | Frontend | 用户反馈线上页面无“上传导入”入口：在 `/costing/shipments` 顶部补齐上传控件，直接调用后端 `POST /api/planner/shipments/import` 并导入完成后自动选中批次刷新数据。 | ✅ `ShipmentMonitorPage` 增加“上传发货单（xlsx 导入）”卡片（文件/导出日期/requested_by）；新增前端 API `importShipmentsXlsx`；导入成功后自动设置 `selectedBatchId` 并 `invalidateQueries(['shipments'])` 刷新批次/异常/快照。`npm -C frontend run build` 通过。 | 下一步：部署最新前端静态文件到 47.99.89.206（若生产仍未看到上传入口）。 |
| 2025-12-22 | 发货导入入口（上线派单） | Planner（Hub Agent） | 线上 `/costing/shipments` 未看到上传入口，根因通常是前端静态未部署到含 `37e91b9` 的版本；需运维按既有静态发布流程上线。 | ✅ 新增运维闭环任务单 `DOC/agents/briefings/ops_deploy_frontend_shipments_upload_to_4799.md`（git pull→frontend build→deploy_static→页面验证）。 | @Ops Agent / @Backend Ops 执行上线并回填最小证据（git log/页面确认/OpenAPI True）。 |
| 2025-12-22 | 发货异常队列（按Excel列可读化） | Frontend + Backend | 用户反馈异常队列显示系统字段（batch_id/reason）业务同事看不懂；要求按发货单Excel列结构展示并补必要系统字段。 | ✅ Backend：`GET /api/planner/shipments/exceptions` 返回中补齐对应 `shipment_lines` 字段（row_index/shipment_no/sku_code/spec_text/qty/revenue_amount/channel/completed_at/spec_hash）。✅ Frontend：异常表格改为“时间/发货单号/SKU/交易规格/数量/金额/原因/消息”，隐藏 batch_id 等系统字段。`pytest backend/tests/planner/test_shipment_import_bom_snapshots_mvp.py -q`、`npm -C frontend run build` 均通过。 | 需部署到 47.99.89.206 才能在页面生效。 |
| 2025-12-22 | 夜间扣库性能口径（聚合/缓存/幂等） | Planner（Hub Agent） | 用户担心“5000 单/天 × 50 物料/单”夜间脚本会爆，并提出“同一 SKU 大量重复订单是否可一起扣”。 | ✅ 结论：可行且应当聚合。建议执行口径：1）计算前先 resolve SKU→（模型/版本）并做批内缓存；2）按 `version_id + spec_hash` 聚合 BOM 计算（同 SKU 规格重复可显著减少计算次数）；3）扣库阶段按 `material_id` 再聚合写入库存流水，避免逐单逐行扣导致写放大/锁竞争；4）作业必须幂等（以 batch_id/shipment_line_id 或 deduction_job_id 作为唯一键）并支持断点续跑。 | 下一步：单开闭环做“夜间扣库作业 MVP（聚合扣减 + 幂等重跑 + 作业报告）”。 |
| 2025-12-22 | SKU 主档（ERP 商品列表）口径确认 | Planner（Hub Agent） | 用户明确两张表关系：发货单无平台规格Id，唯一稳定关联键为“货品条码（系统）”；平台规格Id仅用于展示/追溯。希望先导入部分 SKU 主档测试匹配可行性，再在发货导入时自动触发/自动回写主档。 | ✅ 口径定死：SSOT=货品条码（系统）；发货导入优先读 SKU 主档，未命中则以发货行回写最小主档。新增派单：后端 `backend_sku_master_import_and_autobind_mvp` + 前端 `frontend_sku_master_workspace_mvp`。 | 下一步：@Backend Agent/@Frontend Agent 各自按任务单闭环交付并验收。 |
| 2025-12-23 | SKU 主档预解析缓存 + 规格差异标记（避免重复解析） | Planner（Hub Agent） | 用户提出：希望 SKU 主档提前沉淀“我们系统的匹配/尺寸/解析结果”，减少发货触发时重复解析；并关心“发货触发时交易规格与主档规格不同”是否需要重新自动匹配。 | ✅ 已实现：在 `sku_master.metadata_json` 写入 ERP 规格的 `erp_spec_hash/erp_parser_version/erp_dimensions/erp_tokens/model_code_hint_erp`；发货导入时记录 `last_shipment_spec_text/hash/dimensions/tokens/model_code_hint_shipment` 并标记 `spec_mismatch`（不覆盖 ERP 字段，不自动改绑定）。`GET /sku-master` 列表/详情直接回传 computed 字段，前端列表显示“对接状态+规格差异”，详情显示解析摘要。 | 下一步：若要“规格差异时自动重绑”，需增加 guardrails（唯一命中才自动写入绑定、已有绑定不覆盖、异常队列复核）。 |
| 2025-12-23 | ERP 回传工艺/生产规格需求收口（MVP） | Planner（Hub Agent） | 用户提出：需要把我方系统生成的“详细生产工艺/可生产规格”回传到ERP（工人看ERP字段），因为ERP交易规格不足以说明工艺。 | ✅ 产出：`DOC/costing/blueprints/erp_writeback_process_spec_mvp.md`，明确写回对象（订单明细行优先/其次SKU/工单）、必须字段（production_spec_text/process_instructions_text + trace_id/spec_hash/model_code/version_label）、幂等键与不覆盖原则，以及API/无API(导入模板)两种落地路线。 | 下一步：确认ERP可写入的目标字段/对象（订单明细行自定义字段/备注/工艺字段），再开后端闭环实现“写回任务表+批量导出或API调用”。 |
| 2025-12-23 | 对外谈判资料：吉客云/ERP API 需求表单 v1 | Planner（Hub Agent） | 对方不开放试用 API，用户要求把“我们需要什么能力才能满足需求”写成可谈判的材料，尤其包含“回写生产工艺/规格”。 | ✅ 产出：`DOC/costing/blueprints/jky_api_requirements_form_v1.md`，表格化列出鉴权/限流、SKU主档读接口、发货单/明细读接口（必须含交易规格+明细行唯一键）、退货/作废接口、以及写回接口（订单/发货明细行优先，支持批量+幂等键）。 | 下一步：与对方确认“写回目标对象/字段位置/是否支持批量与幂等”，据此落地后端 write-back 作业。 |
| 2025-12-23 | 对外谈判资料：售后/退货（冲销）需求表单 v1 | Planner（Hub Agent） | 用户补充：售后退货也需要表单与接口谈判材料，用于后续冲销扣料/成本对账。 | ✅ 产出：`DOC/costing/blueprints/jky_after_sales_returns_requirements_form_v1.md`，明确售后语义（仅退款/退货退款/换货/补发/作废）、必须主键（after_sales_line_id）与“必须映射回原始订单/发货明细行 related_line_id”，并列出增量/分页/状态变更、以及可选写回字段。 | 下一步：对方确认是否能提供 `after_sales_line_id + related_*_line_id`，否则需给稳定组合键与导入模板方案。 |
| 2025-12-23 | 产品模型清单（打样/标准） | Frontend | 用户反馈：替换虚拟物料（如 VM00023→VM00022）后，BOM单价/单位未更新，仍沿用旧行快照。 | ✅ `ProductModelEditorDrawer.tsx`：虚拟物料选择时改为读取 `fetchVirtualMaterial(id)` 详情并回填 `metadata_json.bom_unit_price/bom_unit`；`bom_unit_price` 优先取虚拟物料详情字段，缺失则按 bindings×真实物料 BOM 单价（含 loss）汇总推导。并已重新 `npm -C frontend run build` + 发布静态资源到 `/var/www/html/ai-costing/dist`。 | 若线上仍看不到变化：浏览器强刷/清缓存，确认加载到最新 `ProductModelEditorDrawer-*.js` chunk；如仍异常再对比 VM 主数据是否缺 bindings 或价格字段。 |
| 2025-12-23 | 工艺模板（物料组/工序组口径统一） | Frontend | 用户要求：计量方式默认从物料/工序主数据带出；若人工修改，必须按 BOM 单位限制候选（米→周长/宽度/高度；平米→面积；个/套→数量），物料组与工序组同逻辑。 | ✅ `ProcessModulesPage.tsx`：物料组“计量方式”下拉按当前 BOM 单位动态过滤，并在单位变化导致不合法时自动纠偏；工序组新增“计价量类型”列（写入 `metadata_json.measure_type`），同样按单位过滤候选并自动纠偏；兼容历史 `length`（仅当旧数据已选中时展示为 legacy，不影响新口径）。前端已 `npm -C frontend run build` 并发布静态资源。 | 若需要彻底移除 `length`：需先批量迁移历史数据（把 length 映射到 perimeter/width/height 之一）再删兼容分支，避免老模板掉值或计价量改变。 |
| 2025-12-23 | 打样/标准模型清单（计量方式口径统一） | Frontend | 用户指出：需要在 `/costing/sample-models` 与 `/costing/standard-models` 共用的“清单编辑”中统一计量方式规则（不是工艺模块页）。 | ✅ `ProductModelEditorDrawer.tsx`：物料行“计量方式”下拉改为仅展示该 BOM 单位允许的候选（米→周长/宽度/高度；平米→面积；个/套→数量），并对不兼容历史值保留 legacy 选项避免回显空；工序行“计量方式”同样按单位限制候选，且在选择工序时把工序库的 `unit_of_measure` 快照落到 `metadata_json.measure_unit` 以便做单位判断；已重新 build 并发布静态资源。 | 若需要强制纠偏老数据（自动改值而非 legacy）：建议先离线跑一次迁移脚本并由业务确认映射，否则会改变历史模板/版本的成本口径。 |
| 2025-12-23 | 打样/标准模型清单（单位→计量方式 bugfix） | Frontend | 用户反馈：BOM 单位为“平米”时仍出现“周长”，原因是单位判断逻辑把“平米”误判为“米”（包含“米”字）。 | ✅ 修复 `ProductModelEditorDrawer.tsx`：`allowedCalcMethodsByBomUnit` 改为基于 `normalizeUnit()` 的等值判断（平米/米/个/套），避免“平米⊂米”的包含关系误判；并在 UI 编辑态对不合法的当前值自动纠偏到允许的默认值（保存才落库）。已重新 build 并发布静态资源（入口 `index-CpaLWpgm.js`）。 | 如仍看到旧行为：请强制刷新（Ctrl+F5 / Cmd+Shift+R）或用 `?v=20251223` 绕缓存，并确认 Network 加载到 `index-CpaLWpgm.js`。 |
| 2025-12-23 | 标准模型列表（按主模型展示） | Frontend | 用户反馈：从打样推导标准版本后，`/costing/standard-models` 列表出现同一模型的多个标准版本行（如 `OZU-STANDARD-20251223-01/-02`），但期望该页只展示“主模型”，版本列表应在抽屉 Tab 内查看。 | ✅ `StandardModelsPage.tsx`：从“按标准版本分页列表”改为“按主模型列表（ProductModel）”；列表仅展示 `standard_version_count>0` 的模型（与打样管理一致风格），点击进入抽屉后在“标准模型管理 Tab→标准版本”查看具体版本。已重新 build 并发布静态资源。 | 如需在列表显示“最新标准版本/草稿数量”等摘要，可在后续迭代增加列（不改变“主模型一行”的口径）。 |
| 2025-12-23 | 标准版本 Tab（发布/删除/汇总） | Frontend | 用户要求：标准模型管理 Tab “标准版本”里，物料数/工序数/物料价/工序价/制造费/合计价在保存后应回填显示；操作列增加“删除/发布”；同一模型只允许一个版本发布，且发布后不可编辑/删除。 | ✅ `ProductModelEditorDrawer.tsx`：保存清单后自动 `computeVersionStats + versionsQuery.refetch`，版本表上方增加汇总条（显示当前选中版本的统计）；标准版本列表“操作”列新增 **发布/删除**（仅 draft 可编辑/发布/删除；发布会按后端语义自动归档其它已发布版本）。前端已 build 并发布静态资源（入口 `index-NJKG9iLx.js`）。 | 如需“发布后整个清单编辑只读”（禁用表格输入）可后续补一轮 UI 锁定；当前先把保存/编辑入口卡死为 draft，避免误操作。 |
| 2025-12-23 | 标准版本 Tab（深复制：含高级公式/变体） | Frontend | 用户要求：标准版本列表在“发布”后面增加“复制”，复制成一个新版本且内容一模一样（包含高级计算公式、Overlay 变体等）。 | ✅ `ProductModelEditorDrawer.tsx`：标准版本表格操作列新增“复制”；复制时创建新草稿版本并复制：1）源版本 `metadata_json`（含 calc_mode/fixed_price 等），2）整份版本清单（materials/processes/spec，含 derive_template 等高级公式字段），3）复制 Overlay 行级变体（`line-variants`），并按物料行顺序映射 `base_line_id` 到新版本对应行。前端已 build 并发布静态资源（入口 `index-MrqsFjO_.js`）。 | 备注：若未来支持“同一物料重复多行且顺序变更”的复杂场景，建议后端在复制版本时返回 stable mapping，前端无需按行序推断。 |
| 2025-12-23 | 标准版本删除（draft 可删） | Backend + Frontend | 用户反馈：标准版本 TAB 删除未发布版本时报错“仅允许删除打样版本（sample）”；期望规则：未发布（draft）标准版本允许删除，已发布/已归档不允许删。并要求操作列加宽。 | ✅ 后端 `DELETE /api/planner/product-model-versions/{id}`：允许 `standard + draft` 删除（软删除/归档），非 draft 标准版本返回 400；保持 sample 删除原规则（若已生成标准版本则不允许删）。✅ 前端标准版本列表：删除文案改为“未发布(draft)可删”，操作列加宽约 1/3。已重启 `planner-costing.service` 并验证 204 删除返回。 | 若未来需要“发布中(publishing)”状态：建议后端新增显式状态并按状态禁删，前端直接按 status 禁用按钮。 |
| 2025-12-23 | 标准版本 Tab（UI 收口） | Frontend | 用户反馈：版本 Tab 顶部“新增版本”后面的“复制版本”按钮不清晰（不知道复制谁）；标准版本列表上方汇总条与“口径固定 100×100×1”提示无用。 | ✅ `ProductModelEditorDrawer.tsx`：移除“新增版本”旁的模糊“复制版本”按钮（保留每行操作里的复制）；删除标准版本列表上方汇总条；移除“发布/SKU绑定”区域的固定口径提示。前端已 build 并发布静态资源（入口 `index-DXaas_Wz.js`）。 | 如需再次提供“复制当前选中版本”入口，建议用二次确认弹窗明确来源版本，并显示版本名/状态，避免歧义。 |
| 2025-12-23 | SKU 主档工作台（导入/查询/命中率） | Frontend | 按派单新增 `/costing/sku-master`：导入一部分 SKU 主档并验证字段齐全/匹配可行性。 | ✅ 新增页面 `SkuMasterWorkspacePage.tsx`（上传导入 + 分页列表 + 详情抽屉 + 图片预览 + metadata_json）；新增前端 types/services：`SkuMaster*`、`importSkuMasterXlsx/fetchSkuMaster/fetchSkuMasterDetail`；并挂载路由与菜单入口。`npm -C frontend run build` 通过。 | 后续：若需要“全量命中率”（非当前页）/按字段缺失分桶，需要后端补统计接口或前端拉全量后聚合（注意性能）。 |
| 2025-12-23 | SKU 主档工作台上线（47.99.89.206） | Backend Ops | 线上访问 `/api/planner/sku-master` 报 500，需要按任务单完成后端迁移 + 重启，并验证 OpenAPI 与列表接口返回 200；同时更新前端静态资源以暴露入口页面。 | ✅ 目标机：`git pull --ff-only` 后包含 `ee01e55/9037684`；`alembic upgrade heads` 执行 `0017 -> 0018_sku_master_import_mvp`；设置 `XDG_RUNTIME_DIR=/run/user/1000` 重启 `planner-costing.service` 成功；OpenAPI 校验 `/api/planner/sku-master` + `/api/planner/sku-master/import` → `True`；`GET http://127.0.0.1:8800/api/planner/sku-master?page=1&page_size=10` → 200（空列表）；前端静态已发布至 `/var/www/html/ai-costing/dist`。 | 后续：导入 `ERP 理 平台商品列表.xlsx` 形成首批主档数据；如需统计“全库命中率/字段齐全率”，建议新增后端聚合接口避免前端拉全量。 |
| 2025-12-24 | 宜搭物料同步：新增“采购单价/单位”字段（不覆盖入库字段） | Backend + Frontend | 用户澄清：本系统已将原字段口径化为“入库单价/单位”（落库仍为 `materials.unit_price`/`materials.purchase_unit`）；宜搭新增“采购单价/单位”为新字段，仅需同步到本地并在物料抽屉展示，不能覆盖原入库字段。 | ✅ 后端：`backend/config/yida_materials.json` 保持 `unit_price/purchase_unit` 继续映射旧字段（入库口径）；新增 `yida_purchase_unit/yida_purchase_unit_price/purchase_to_inbound_formula` 映射并写入 `materials.metadata_json`（`backend/src/planner/services/yida_sync.py`）。✅ 已用 `backend/venv` dry-run 验证同步不会覆盖入库字段，dump 中新字段可见。✅ 前端：物料抽屉基础信息同时展示“入库单价/单位（本地字段）”与“采购单价/单位（宜搭新字段）”，并展示“采购转入库公式”。前端 build 通过并已部署，线上入口 `index-YGqcR04n.js`。 | 生产定时同步：确保 systemd/cron 使用最新 `backend/config/yida_materials.json`；可用 `source backend/venv/bin/activate && python backend/scripts/sync_yida_materials.py --config backend/config/yida_materials.json --limit 2 --dry-run` 手动验证。 |
| 2025-12-25 | 物料：单物料同步宜搭 + 口径修正（不兜底单位；不从宜搭同步 BOM 单价/单位） | Backend + Frontend | 用户明确：单位不能靠系统猜测（需要参与换算）；宜搭字段口径为：采购单位 `selectField_mjjgsdlp`、采购单价 `numberField_mjjgsdlr`、入库单位 `selectField_lxo1y6ac`、入库单价 `numberField_lxmosz1s`。并要求：**不要从宜搭同步 BOM 单价/单位**，本地 BOM 单价/单位由“入库→BOM 换算”推导。 | ✅ 撤销 UI 单位兜底：单位缺失就显示 `-`，不再按 `calculation_method` 自动补齐。✅ 后端：`/base-config/materials/sync-yida` 支持 `material_codes` 过滤（写入 job.payload，`run_material_sync_job` 传入 `MaterialSyncService.sync_materials(material_codes=...)`），避免全量几千条。✅ 映射修正：`purchase_unit` 绑定 `selectField_lxo1y6ac`、`unit_price` 绑定 `numberField_lxmosz1s`；采购字段写入 `metadata_json.yida_purchase_*`；并显式禁用 `unit/bom_unit_price` 的宜搭同步（`yida_materials.json` 置空 + `yida_sync.py` 移除写入，添加“口径约束”备注）。✅ 前端：抽屉标题区保留“同步宜搭（本物料）”按钮，触发 filtered sync job 并轮询后刷新当前物料。前端 build+部署完成，线上入口 `index-VwrKkmlr.js`。 | 下一步：请 Ops/Planner 从宜搭后台确认并补齐“BOM单位”控件 ID（如果业务确实需要从宜搭同步 BOM 单位）；但即使补齐，也必须保持 **BOM 单价** 不从宜搭同步。 |
| 2025-12-25 | 物料详情抽屉：成本参数布局优化 + 单位选项口径统一 | Frontend | 用户反馈：成本参数里选“面积”后 BOM 单位出现 `m2` 等非规范值；并要求在成本参数中将“计算方式/BOM单位”“入库→BOM换算/BOM单位”“BOM→库存换算/库存单位”放到同一行展示，同时把“保存/新建指南/取消”移动到抽屉标题栏并置于“同步宜搭”之前。 | ✅ 统一单位 options：`frontend/src/constants/calculationMethods.ts` 将单位 value 统一为 `平米/米/个/套`（禁止再用 `㎡/m` 作为 value，避免回归）。✅ 抽屉布局：`MaterialMasterPage.tsx` 成本参数将“计算方式 + BOM单位”同区展示；“入库单价/单位（只读）”放到“采购规格（只读）”下方；“入库→BOM 换算 + BOM单位（同上）”同一行；“BOM→库存换算 + 库存单位”同一行。✅ 抽屉标题栏：将“保存/新建指南/取消”上移到标题栏，并把“同步宜搭（本物料）”改名为“同步宜搭”且排在按钮组最后。前端 build+部署完成，线上入口 `index-d68Wp0Sw.js`。 | 注意：BOM 单价/单位依旧只由“入库→BOM 换算”推导，本次不涉及宜搭同步。 |
| 2025-12-25 | 物料详情抽屉：基础信息TAB补字段 + 列宽调整 | Frontend | 用户要求：基础信息TAB按指定顺序补齐字段，并新增“入库→BOM 换算（从成本参数复制）”“采购规格（从成本参数复制）”；同时将物料列表“物料编码”列宽缩小 1/2。 | ✅ `MaterialMasterPage.tsx`：基础信息TAB新增“入库→BOM 换算”“采购规格”，并按用户给定顺序重排展示；物料列表“物料编码”列宽由 160 调整为 80。前端 build+部署完成，线上入口 `index-CSnEuhjs.js`。 | 备注：BOM 单价/单位仍只由本地换算推导，基础信息TAB仅展示换算系数，不参与宜搭同步。 |
| 2025-12-25 | 物料换算：BOM→库存自动推回（倒数） | Frontend | 用户需求：库存盘点用采购/入库单位；已知“入库→BOM 换算（1 入库单位 = x BOM单位）”，希望系统自动给出“BOM→库存换算（1 BOM单位 = ? 库存单位）”应填多少（推回去/取倒数），并自动显示/回填。 | ✅ `MaterialMasterPage.tsx`：当库存单位=入库单位时，自动计算 `conversion_bom_to_inventory = 1 / conversion_purchase_to_bom` 并在未手填时自动回填；同时在输入框下方显示推导结果（例如：入库→BOM `1米=1.32平米` ⇒ BOM→库存自动推导 `1平米=0.757576米`）。前端 build+部署完成，线上入口 `index-BwUeFKBh.js`。 | 若库存单位≠入库单位（例如库存单位为“包/箱”），系统不会猜测，需要人工维护 BOM→库存换算或先补齐单位体系。 |
| 2025-12-25 | 物料成本参数面板收口（布局/文案极简） | Frontend | 用户要求：成本参数面板删除多余说明/提示/（只读）/自动推导文案；将 BOM单价+计算方式 同一行；删除“BOM单位（同上）”并用 BOM单位下拉替代；采购规格与入库单价同一行。 | ✅ `MaterialMasterPage.tsx`：BOM单价/单位 与 计算方式同一行；采购规格 与 入库单价/单位同一行并去掉“(只读)”字样；删除 “= 入库单价 ÷ 入库→BOM…” 等多余文案、删除计算方式提示文案、删除自动推导显示文案、删除整块“说明”Alert、删除本地描述 extra 提示；“入库→BOM 换算”右侧改为 BOM单位下拉。前端 build+部署完成，线上入口 `index-C_iSRvBx.js`。 | 自动回填逻辑保留，但不再在界面展示推导提示文案。 |
| 2025-12-25 | 成本参数：BOM→库存换算/库存单位只读 | Frontend | 用户要求：成本参数面板中“BOM→库存换算”“库存单位”两个输入框改成只读框（仍由系统推回/默认入库单位，不允许手改）。 | ✅ `MaterialMasterPage.tsx`：将 `conversion_bom_to_inventory` 与 `inventory_unit` 两个控件设为 disabled 只读；库存单位默认固定为入库单位（`purchase_unit`）。前端 build+部署完成，线上入口 `index-CwUkADZ5.js`。 | 如未来存在“库存单位≠入库单位”的真实场景，需要重新开放编辑或补“入库→库存”独立换算链路。 |
| 2025-12-25 | 基础信息TAB：显示本地描述 | Frontend | 用户确认：基础信息里的“备注”不一定来自成本参数“本地描述”；要求无论是否相同，都展示“本地描述”的值。 | ✅ `MaterialMasterPage.tsx`：基础信息TAB将原“备注”更名为“备注（宜搭）”，并新增一行“本地描述”（展示 `metadata_json.local_description`，来源于成本参数输入）。前端 build+部署完成，线上入口 `index-COZ5A2v7.js`。 | — |
| 2025-12-25 | 基础信息TAB：备注（宜搭）无值隐藏 | Frontend | 用户反馈“备注（宜搭）没有值，估计是假的”，要求先隐藏。 | ✅ `MaterialMasterPage.tsx`：当 `bom_notes` 为空时不渲染“备注（宜搭）”行。前端 build+部署完成，线上入口 `index-F_75whEu.js`。 | 后续如确认宜搭字段映射无误，可恢复显示。 |
| 2025-12-25 | 宜搭同步：启用字段接入 `is_active` | Backend | 用户询问“物料启用 radioField_lnk335sn 是否同步”；并希望列表默认仅显示启用物料（“仅启用”开关有意义）。 | ✅ 更新 `backend/config/yida_materials.json`：将 `is_active` 映射到宜搭 `radioField_lnk335sn`；`status` 不从宜搭同步，默认由 `is_active` 推导（active/inactive）。已触发一次同步 job（limit=50）验证成功。 | 前端“仅启用”开关继续保留：默认只展示启用物料（来自宜搭/本地均适用）。如需“严格只读宜搭启用状态”，再考虑禁用本地停用开关。 |
| 2025-12-25 | 物料页：宜搭同步拆分按钮（全量/仅拉新/仅更新原价格） | Frontend+Backend | 用户问“同步宜搭到底同步什么”，并希望新增“同步新物料”“更新原价格”两个按钮减少误覆盖。 | ✅ 后端 `sync-yida` 支持 `mode=full/new_only/core_fields`；`core_fields` 只更新入库单价/单位、采购单价/单位、采购→入库换算、采购规格（raw_form_data 只 merge 对应 fieldId），`new_only` 仅新增不更新；前端 `/costing/materials` 增加 3 个按钮并分别传入 mode，原“同步宜搭”改名为“全量同步宜搭”。已 build+部署；并重启 `planner-costing.service` 生效。 | 如后续需要“只更新某个物料的原价格”，可在现有“同步宜搭(本物料)”按钮上加 mode 下拉。 |
| 2025-12-25 | 物料页：推导 BOM 价格（入库→BOM） | Frontend+Backend | 用户要求新增“推导BOM价格”按钮，并希望更新价格后 BOM 价格能自动刷新；同时希望无法推导时在 BOM 单位处红字提示原因。 | ✅ 新增 `POST /api/planner/base-config/materials/derive-bom-prices`（后台任务写入 `metadata_json.bom_unit_price`）；前端 `/costing/materials` 在“刷新”后新增“推导BOM价格”，并在三种宜搭同步完成后自动触发一次推导；BOM 单位无法推导时红字+Tooltip 提示原因（未选 BOM 单位/入库单价缺失/换算系数不合法）。 | 下游引用（虚拟物料/工艺模板/产品模型）建议采用“快照+显式刷新”的专业流程：发布版本不回写，草稿版可点“刷新价格”生成新快照。 |
| 2025-12-25 | 推导BOM价格：强制重算 + 原因直显 | Frontend+Backend | 用户反馈“有换算=2.8、BOM单位=平米但仍为红色 '-' 且无提示”，需要更明确的失败原因展示，并确保更新入库价后推导能覆盖旧快照。 | ✅ 后端推导改为**每次按最新入库单价/换算系数强制重算并覆盖**（不沿用旧 `bom_unit_price`）；同时补齐严格校验：缺 BOM 单位/缺入库单位/入库单价≤0/换算≤0 则不推导并清理旧快照。前端列表在红字情况下**直接显示原因一行**（不依赖 hover）。 | 线上看到红字时按原因补齐：先同步/录入入库单位+入库单价，再补齐“入库→BOM换算”和“BOM单位”，再点“推导BOM价格”。 |
| 2025-12-25 | 全局任务列表（Task Center） | Frontend+Backend | 用户反馈后台任务（同步/推导）切页后“没反应”，容易重复点击；希望全项目通用任务列表，顶部入口+抽屉查看状态/耗时/完成时间。 | ✅ 后端新增 `GET /api/planner/task-center/recent?limit=...` 聚合返回 `PlannerJob + MaterialSyncJob`；前端顶部“Planner 控制台”右侧新增“任务列表”入口（运行中数量 Badge + 动画提示），点击打开抽屉查看任务状态/耗时/进度/错误信息，自动轮询运行中任务。 | 后续可加“点击任务→详情页/复制ID/一键跳转到相关页面”，以及对同类任务做去重/防抖。 |
| 2025-12-25 | 任务列表增强：点击任务→详情抽屉（复制ID/跳转/结果） | Frontend | 用户确认“下一步更强：点击某条任务→打开详情（复制任务ID、跳转到对应页面、显示 result_json）”可做。 | ✅ 任务列表抽屉中点击任一任务行，将打开对应详情：Planner 任务复用 `PlannerJobDrawer`（新增 `extraActions` 支持“跳转”按钮）；物料同步/推导任务新增 `MaterialSyncJobDrawer`，支持复制任务ID、查看 payload/result_json、跳转到物料管理。 | 后续可加：从 task-center item 直接携带 payload/result 省一次详情请求；为任务增加“取消/重试”能力。 |
| 2025-12-25 | 标准模型列表：实时核价入口 + 偏差预警列（±5%）+ 删除按钮（受限） | Frontend | 用户担心遗忘：要求在标准模型列表“操作”增加“实时核价”，列表加一列偏差，超过 ±5% 预警；并要求增加删除按钮。 | ✅ `/costing/standard-models`：新增“实时核价”按钮（默认按 1000mm×1000mm×1 调 `POST /product-models/{id}/preview`），对比“已发布标准版本”在 `version.metadata_json.ui_stats` 保存的基准价，输出偏差列；偏差绝对值≥5% 标红预警。删除按钮按后端保护规则默认禁用（存在 standard 版本时不允许删除，避免 SKU 绑定断裂）。 | 若需“已生成 standard 版本仍可删除/归档”，需单独评审后端保护规则与 SKU 绑定影响；也可改为“归档/停用”而非删除。 |

## 调试模板

```
### YYYY-MM-DD – <模块/任务>
- 问题：
- 假设：
- 操作：
- 验证：
- 结果：
- 遗留/待办：
```



